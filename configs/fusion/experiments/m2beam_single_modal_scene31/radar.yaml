_base_: _base.yaml
experiment:
  name: m2beam_scene31_radar_only
model:
  modalities: [radar]
  primary:
    modalities: [radar]
    radar_channels: 2
    encoders:
      radar:
        type: radar_cnn
        output_dim: 64
        radar_channels: 2
output:
  run_name: m2beam_scene31_radar_only
