_base_: image_gps_supervised_2604_s32_s34_lowmem.yaml

experiment:
  name: safe_residual_rerank_anchor_only_control_2604_s32_s34_lowmem
  ablation: safe_residual_rerank_anchor_only_control
  seed: 17
model:
  primary:
    reranker:
      enabled: true
      type: safe_residual_beam_reranker
      anchor_top_k: 8
      prior_top_k: 0
      neighborhood_radius: 0
      max_candidates: 8
      max_residual_scale: 0.0
      fallback_policy: never
      diagnostics_mode: full
loss:
  safe_rerank:
    enabled: false
output:
  run_name: safe_residual_rerank_anchor_only_control_2604_s32_s34_lowmem
