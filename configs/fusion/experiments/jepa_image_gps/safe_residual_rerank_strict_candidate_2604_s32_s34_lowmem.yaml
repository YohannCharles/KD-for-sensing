_base_: safe_residual_rerank_teacher_anchor_2604_s32_s34_lowmem.yaml

experiment:
  name: safe_residual_rerank_strict_candidate_2604_s32_s34_lowmem
  ablation: safe_residual_rerank_strict_candidate
model:
  primary:
    reranker:
      anchor_top_k: 10
      prior_top_k: 12
      neighborhood_radius: 2
      max_candidates: 24
      max_residual_scale: 0.35
      min_gate_confidence: 0.02
loss:
  safe_rerank:
    enabled: true
    weight: 0.25
    candidate_ce_weight: 1.0
    pairwise_margin_weight: 0.1
    margin: 0.1
    no_regret_weight: 0.25
    anchor_dba_threshold: 1
output:
  run_name: safe_residual_rerank_strict_candidate_2604_s32_s34_lowmem
