_base_: _base_no_jepa_rbma.yaml
experiment:
  name: no_jepa_rbma_kd
model:
  primary:
    ablation_id: no_jepa_rbma_kd
    use_full_to_partial_kd: true
    kd_teacher_mode: online_full
loss:
  u_mask_beam_jepa:
    use_full_to_partial_kd: true
    kd_teacher_mode: online_full
training:
  use_full_to_partial_kd: true
  kd_teacher_mode: online_full
  lambda_full_to_partial_kd: 0.5
  lambda_feature_kd: 0.1
output:
  run_name: no_jepa_rbma_kd

