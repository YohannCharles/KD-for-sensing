_base_: _base_no_jepa_rbma.yaml
experiment:
  name: proto_only_baseline
model:
  primary:
    ablation_id: proto_only_baseline
    fusion_type: weighted_sum
    use_beam_prototype_alignment: true
loss:
  u_mask_beam_jepa:
    use_beam_prototype_alignment: true
training:
  use_beam_prototype_alignment: true
  lambda_proto: 0.2
  lambda_modality_proto: 0.0
  lambda_supcon: 0.0
output:
  run_name: proto_only_baseline
