_base_: _base_no_jepa_rbma.yaml
experiment:
  name: weighted_sum_mask
model:
  primary:
    ablation_id: weighted_sum_mask
    fusion_type: weighted_sum
    use_modality_uncertainty: false
loss:
  u_mask_beam_jepa:
    use_beam_prototype_alignment: false
    use_full_to_partial_kd: false
    kd_teacher_mode: disabled
output:
  run_name: weighted_sum_mask
