_base_: _base.yaml
experiment:
  name: masktrain_cls
model:
  primary:
    type: modular_sequence
    modalities: [image, radar, gps, lidar]
    image_profile: rgb_imagenet
    image_channels: 3
    radar_channels: 2
    gps_input_size: 3
    lidar_channels: 3
    feature_size: 64
    d_model: 64
    num_classes: 64
    num_pred: 1
    encoders:
      image: {type: tinyvit_5m_scratch_rgb, output_dim: 64, pretrained: false, freeze_backbone: false}
      radar: {type: radar_cnn, output_dim: 64, pretrained: false, freeze_backbone: false}
      gps: {type: gps_mlp, output_dim: 64, hidden_size: 64, dropout: 0.1, pretrained: false, freeze_backbone: false}
      lidar: {type: tinyvit_5m_scratch_rgb, output_dim: 64, pretrained: false, freeze_backbone: false}
    representation_core:
      type: masktrain_mean_fusion
      d_model: 64
      modality_count: 4
    heads:
      beam: {type: beam_head, dropout: 0.1, num_classes: 64}
    paper_metadata:
      model_group: MaskTrain-CLS
      baseline_scope: plain_mask_training_control
      paper_equivalent: false
loss:
  type: cross_entropy
training:
  grad_clip: 5.0
temporal_missing:
  preserve_unmasked_for_superset: false
