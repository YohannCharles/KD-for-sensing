_base_: ../fusion/image_gps_supervised.yaml

difficulty:
  profiles:
    gps_mild_async_train:
      stage: train
      split: train
      condition: gps_mild_async
      severity: 1.0
      seed: 42
      fallback: forward_fill
      operators:
        - type: temporal_delay
          modality: gps
          max_delay_steps: 1

output:
  run_name: fusion_image_gps_mild_async_train
