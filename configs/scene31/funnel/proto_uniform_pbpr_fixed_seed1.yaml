_base_: ../templates/main_v3_proto_es20_base.yaml
experiment:
  name: proto_uniform_pbpr_fixed_seed1
  seed: 1
model:
  primary:
    ablation_id: proto_uniform_pbpr_fixed_seed1
training:
  epochs: 40
  max_epochs: 40
  missing_pattern_sampler: uniform
  prototype_recenter: &id001
    enabled: true
    mode: sanity_fixed
    fallback_empty_class: true
loss:
  u_mask_beam_jepa:
    epochs: 40
    max_epochs: 40
    missing_pattern_sampler: uniform
    prototype_recenter: *id001
evaluation:
  beam_distance_circular: true
output:
  run_name: proto_uniform_pbpr_fixed_seed1
  dir: outputs/scene31_funnel_lmdb
