# Diagnostics Configs

`configs/diagnostics/amber_lite_missing_modality_eval.yaml` 是当前保留的缺失模态 smoke/evaluation diagnostic 输入。输出应写入 ignored `outputs/analysis/` 或显式本地路径。

历史 JEPA visual analysis、GPS shortcut benchmark、Scenario D/CxD、geometry-prior 和 residual-rerank diagnostic manifests 已退役或删除；如需复查只能通过历史 archive 或重新开 OpenSpec change，不恢复同名 current config。
