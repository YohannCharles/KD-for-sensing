_base_: deepsense6g_gps_conditioned_jepa_smoke.yaml
experiment:
  name: jepa_visual_sweep_local_token_mixing_smoke
model:
  primary:
    visual_encoder:
      type: local_token_mixing
      image_channels: 3
      latent_dim: 64
      patch_size: 16
      local_kernel_size: 3
      depth: 1
      num_heads: 4
      max_tokens: 196
      checkpoint_policy: partial_reuse
    predictor:
      max_tokens: 196
output:
  dir: outputs/analysis/jepa_visual_architecture_sweep
  run_name: jepa_visual_sweep_local_token_mixing_smoke
