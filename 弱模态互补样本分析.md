有必要。下面这段可以直接发给 Codex。
我建议把它做成现有可视化网页里的一个新页签：**Complementarity Explorer / 弱模态互补样本分析**。这样它不只是“找几个例子”，而是能系统回答：

* 弱模态是否存在互补样本？
* 这些样本是否被当前融合模型利用上？
* 弱模态造成的负迁移有多少？
* 弱模态有用的样本是否集中在某些条件下？

Gradio 的 `Blocks` 适合做这种多区域、事件驱动的交互界面，`Dataframe` 和 `State` 也适合承载筛选结果表与当前会话状态。([Gradio][1])

```text
请在当前项目中新增一个“弱模态互补样本分析”功能，用于系统识别：
1. 强势模态预测失败但弱势模态预测成功的样本；
2. 弱势模态是否真正帮助融合模型纠正了强势模态错误；
3. 弱势模态是否造成了负迁移；
4. 这些样本是否集中在某些可识别条件下。

====================
一、功能目标
====================

当前研究背景：
- 强势路径：strong_only = gps + mmwave
- 弱势模态：image / radar / lidar
- 当前 Scene32 clean setting 下，弱模态全局平均增益很低，但仍需判断：
  A. 弱模态是否完全低效用；
  B. 还是“全局无收益，但局部条件下有补充价值”。

请实现一个新的分析模块：
- 后端：生成弱模态互补样本统计与逐样本 case 表；
- 前端：在现有 Gradio 可视化网页中新增一个 Tab：
  `Complementarity Explorer`
  中文可显示为：`弱模态互补样本分析`

该功能用于支撑后续研究判断：
- 是否值得做 sample-wise safe fusion；
- 是否存在 conditional utility；
- 当前 fusion 是否没有利用到弱模态已有信息。

====================
二、优先复用的已有产物
====================

优先检查并复用当前已有输出文件：
1. `outputs/scene32/scene32_marf/conditional_utility/subset_predictions.csv.gz`
2. `outputs/scene32/scene32_marf/conditional_utility/conditional_utility_per_sample_delta.csv.gz`
3. `outputs/scene32/scene32_marf/conditional_utility/conditional_utility_by_bucket.csv`
4. 现有可视化网页中用于读取原始样本、多模态输入、标签、预测分布的逻辑

要求：
- 先检查 `subset_predictions.csv.gz` 的真实字段；
- 不要假设字段名，写 schema adapter；
- 如果当前产物里没有概率分布或真值类别概率，请兼容：
  - 最小功能：先基于 top1_pred / y_true 做 case mining；
  - 扩展功能：若有 logits/probs，则额外计算 confidence 相关指标。

====================
三、需要支持的输入配置
====================

界面上支持选择：

1. scene
   - 先支持 scene32
   - 代码结构上预留 scene9

2. horizon
   - 默认 t+1
   - 如果当前任务存在多个未来预测 slot，需要支持下拉选择 horizon：
     - t+1 / t+2 / t+3 / t+4
   - 默认仅展示当前选中的 horizon

3. strong subset
   - 默认：`gps+mmwave`
   - 如果数据中 subset 名称不同，请自动映射或在配置文件中统一

4. weak modality
   - `image`
   - `radar`
   - `lidar`
   - 可选支持 `all weak` 逐个比较

5. fusion subset
   - 对每个 weak modality 对应：
     - `gps+mmwave+image`
     - `gps+mmwave+radar`
     - `gps+mmwave+lidar`
   - 如当前产物 subset 命名不同，请做 adapter

====================
四、核心 case 定义
====================

对每个样本，读取：
- y_true
- strong_pred
- weak_pred
- fusion_pred

并生成以下布尔字段：

1. `strong_correct`
   - strong_pred == y_true

2. `weak_correct`
   - weak_pred == y_true

3. `fusion_correct`
   - fusion_pred == y_true

4. `case_type`
   支持以下类型：

   A. `strong_wrong_weak_correct`
      - strong_correct = False
      - weak_correct = True
      - 含义：弱模态存在潜在互补信息

   B. `strong_wrong_weak_correct_fusion_correct`
      - strong_correct = False
      - weak_correct = True
      - fusion_correct = True
      - 含义：弱模态信息被 fusion 成功利用，完成 rescue

   C. `strong_wrong_weak_correct_fusion_wrong`
      - strong_correct = False
      - weak_correct = True
      - fusion_correct = False
      - 含义：弱模态有信息，但当前 fusion 没有利用上

   D. `strong_wrong_fusion_correct`
      - strong_correct = False
      - fusion_correct = True
      - 含义：融合模型纠正强势路径错误，不要求 weak-only 一定 top1 正确

   E. `strong_correct_fusion_wrong`
      - strong_correct = True
      - fusion_correct = False
      - 含义：弱模态引入负迁移

   F. `all_correct`
   G. `all_wrong`
   H. `other`

注意：
- B 是 A 的子集；
- C 也是 A 的子集；
- 前端统计时要同时支持“互斥大类”和“研究标签”两种查看方式；
- 请避免重复计数造成误解。

====================
五、建议额外计算的逐样本指标
====================

如果能拿到概率或 logits，请计算：

1. `p_true_strong`
   - strong 模型对真实标签 y_true 的概率

2. `p_true_weak`
   - weak 模型对真实标签 y_true 的概率

3. `p_true_fusion`
   - fusion 模型对真实标签 y_true 的概率

4. `weak_gt_gain`
   - p_true_weak - p_true_strong
   - 含义：弱模态相对强模态对真实标签的置信度优势

5. `fusion_gt_gain`
   - p_true_fusion - p_true_strong
   - 含义：融合相对强势路径对真实标签的提升

6. `strong_margin`
   - p_top1_strong - p_top2_strong

7. `weak_margin`
   - p_top1_weak - p_top2_weak

8. `fusion_margin`
   - p_top1_fusion - p_top2_fusion

9. `prediction_disagreement`
   - 例如：
     - strong_pred != weak_pred
     - strong_pred != fusion_pred
   - 若有完整分布，可额外支持 JS divergence / KL divergence，但不是第一优先级

10. 如果已有 `conditional_utility_per_sample_delta.csv.gz`
    - 合并其中现有 per-sample delta 字段
    - 不要重复造轮子

如果没有概率分布：
- 上述概率字段置空；
- 页面仍可正常工作；
- 前端要显式显示 `probability metrics unavailable`，不要静默报错。

====================
六、后端实现要求
====================

请新增一个独立分析脚本，建议路径：

`scripts/analysis/build_complementarity_cases.py`

职责：
1. 读取 subset_predictions；
2. 做 schema 适配；
3. 按 strong / weak / fusion 三类模型对齐样本；
4. 生成逐样本 case 表；
5. 生成全局 summary；
6. 若有 bucket 信息，则生成按 bucket 统计；
7. 输出到指定目录。

建议输出目录：
`outputs/{scene}/complementarity_analysis/`

至少生成以下文件：

1. `complementarity_cases.csv.gz`
   - 每行一个 sample × weak_modality × horizon
   - 包含：
     - sample_id
     - scene
     - horizon
     - weak_modality
     - y_true
     - strong_pred
     - weak_pred
     - fusion_pred
     - strong_correct
     - weak_correct
     - fusion_correct
     - case_type
     - p_true_strong
     - p_true_weak
     - p_true_fusion
     - weak_gt_gain
     - fusion_gt_gain
     - strong_margin
     - weak_margin
     - fusion_margin
     - communication bucket / state fields（如果可用）
     - 原始索引字段

2. `complementarity_summary.json`
   - 全局统计：
     - total samples
     - 每种 weak modality 的 case count / rate
     - rescue rate
     - unused complementary rate
     - negative transfer rate
     - strong wrong rate
     - weak correct among strong wrong rate
     - fusion rescue among strong wrong rate

3. `complementarity_by_bucket.csv`
   - 按 bucket 统计：
     - bucket name
     - weak_modality
     - sample_count
     - strong_wrong_count
     - strong_wrong_weak_correct_count
     - rescue_count
     - unused_complementary_count
     - negative_transfer_count
     - 对应比例
   - 如果当前没有 bucket 字段，则脚本仍能跑，只是不生成该文件或生成空表并记录原因

4. `complementarity_report.md`
   - 自动写一个简短报告：
     - 互补样本是否存在
     - 哪个弱模态互补率最高
     - 融合是否能利用这些互补样本
     - 负迁移是否显著
     - 哪些 bucket 中互补率最高
   - 这里先做模板化，不需要复杂自然语言

实现细节：
- 写成模块化函数：
  - `load_subset_predictions()`
  - `normalize_schema()`
  - `build_case_table()`
  - `compute_summary()`
  - `compute_bucket_summary()`
  - `write_outputs()`
- 增加命令行参数：
  - `--scene`
  - `--input-path`
  - `--output-dir`
  - `--strong-subset`
  - `--weak-modalities`
  - `--horizons`
- 需要日志：
  - 输入字段
  - 实际采用的 schema mapping
  - 输出样本数
  - 缺失概率字段时的 warning

====================
七、前端页面设计
====================

在现有 Gradio app 中新增 Tab：
`Complementarity Explorer`

建议布局：

--------------------------------
顶部：筛选区
--------------------------------
控件：
1. Scene 下拉框
2. Horizon 下拉框
3. Weak modality 下拉框
4. Case type 多选框
5. Bucket 下拉框（如果存在）
6. 排序字段下拉框：
   - sample_id
   - weak_gt_gain desc
   - fusion_gt_gain desc
   - p_true_strong asc
   - p_true_weak desc
   - strong_margin asc
7. 最小阈值：
   - min weak_gt_gain
   - min fusion_gt_gain
8. 按钮：
   - Apply filters
   - Reset filters
   - Export filtered CSV

--------------------------------
中部左侧：统计面板
--------------------------------
显示当前筛选下：
- total samples
- strong wrong count / rate
- strong wrong + weak correct count / rate
- rescue count / rate
- unused complementary count / rate
- negative transfer count / rate
- 可用时显示：
  - avg weak_gt_gain
  - avg fusion_gt_gain

建议使用：
- Markdown cards 或 JSON 输出均可
- 不要只给 raw table，最好让关键指标一眼可见

--------------------------------
中部右侧：统计图
--------------------------------
至少支持：
1. case_type 条形图
2. 按 bucket 的 rescue rate / negative transfer rate 条形图
3. 可选：
   - weak_gt_gain 分布
   - fusion_gt_gain 分布

--------------------------------
下部：样本表
--------------------------------
使用 Dataframe 展示 filtered cases：
默认列：
- sample_id
- weak_modality
- case_type
- y_true
- strong_pred
- weak_pred
- fusion_pred
- p_true_strong
- p_true_weak
- p_true_fusion
- weak_gt_gain
- fusion_gt_gain
- bucket

点击某行后，联动下方样本详情区。

--------------------------------
底部：样本详情区
--------------------------------
复用现有样本可视化组件，展示：
1. 原始模态：
   - image
   - lidar
   - radar
   - gps
   - mmwave
2. 处理后模态（如现有 viewer 已支持，则直接复用）
3. 当前样本真实标签：
   - y_true
4. 三类模型预测：
   - strong-only
   - weak-only
   - fusion
5. 各自：
   - top1
   - top-k
   - 真值标签概率
6. 64 类 beam 分布：
   - strong
   - weak
   - fusion
   - 真实标签用竖线标出
7. 额外字段：
   - bucket
   - per-sample delta
   - 是否属于 rescue / unused complementary / negative transfer

====================
八、建议的默认分析逻辑
====================

默认打开页面时：
- scene = scene32
- horizon = t+1
- weak_modality = image
- case_type 默认选中：
  - strong_wrong_weak_correct
  - strong_wrong_weak_correct_fusion_correct
  - strong_wrong_weak_correct_fusion_wrong
  - strong_correct_fusion_wrong
- 默认排序：
  - 若有概率字段：按 `weak_gt_gain` 降序
  - 否则按 `sample_id`

重点希望用户能快速看出：
1. 强错弱对样本是否很多；
2. 这些样本是否被 fusion 救回来；
3. 负迁移样本是否更多；
4. 哪些弱模态最有互补潜力。

====================
九、必须支持的研究指标
====================

请在 summary 中明确输出以下指标：

1. `complementarity_rate`
   = count(strong_wrong_weak_correct) / count(strong_wrong)

2. `rescue_rate_given_complementary`
   = count(strong_wrong_weak_correct_fusion_correct) / count(strong_wrong_weak_correct)

3. `unused_complementary_rate`
   = count(strong_wrong_weak_correct_fusion_wrong) / count(strong_wrong_weak_correct)

4. `negative_transfer_rate`
   = count(strong_correct_fusion_wrong) / count(strong_correct)

5. `net_fusion_gain_count`
   = count(strong_wrong_fusion_correct) - count(strong_correct_fusion_wrong)

6. 如果有 probability：
   - mean weak_gt_gain
   - mean fusion_gt_gain
   - 各 case type 下的 mean gain

这些指标要按：
- 全局
- weak modality
- bucket
分别统计。

====================
十、需要特别注意的研究解释
====================

请不要把 “strong_wrong_weak_correct” 直接解释成“弱模态整体有用”。

正确解释：
- 它只说明弱模态存在潜在互补信息；
- 是否值得做 sample-wise fusion，还需要同时看：
  1. 该类样本占比是否足够；
  2. 是否集中在可识别条件下；
  3. fusion 是否能利用这些信息；
  4. 负迁移是否会抵消收益。

因此页面上建议加一段固定说明：
- `strong_wrong_weak_correct` = 潜在互补
- `rescue` = 当前融合已利用
- `unused complementary` = 弱模态有信息但融合未利用
- `negative transfer` = 融合破坏了强模态已正确的样本

====================
十一、测试要求
====================

请增加测试，至少覆盖：

1. case 判定逻辑正确
   - 手工构造 8~10 个样本
   - 检查每类 case 是否分类正确

2. summary 指标计算正确
   - complementarity_rate
   - rescue_rate_given_complementary
   - unused_complementary_rate
   - negative_transfer_rate

3. 缺少 probability 字段时仍可运行
   - 概率相关字段为 NaN
   - 不影响 top1 case mining

4. subset schema 不同命名时 adapter 有效

5. 前端筛选函数测试
   - case_type
   - weak_modality
   - horizon
   - bucket
   - sort

====================
十二、验收标准
====================

完成后我希望能够做到：

1. 打开网页，在 `Complementarity Explorer` 中选择：
   - scene32
   - t+1
   - image / radar / lidar
2. 一眼看到：
   - 强错弱对比例
   - rescue 比例
   - unused complementary 比例
   - negative transfer 比例
3. 能筛选出：
   - `strong_wrong_weak_correct`
   - `strong_wrong_weak_correct_fusion_correct`
   - `strong_wrong_weak_correct_fusion_wrong`
   - `strong_correct_fusion_wrong`
4. 点击任意样本，看到：
   - 原始五模态
   - strong / weak / fusion 的预测
   - 真值 beam
   - 64 类分布
5. 导出筛选后的 CSV
6. 能通过自动化测试

====================
十三、实现优先级
====================

P0：
- 后端 case mining
- summary 统计
- 前端筛选表格
- 样本点击联动现有 viewer

P1：
- probability 指标
- bucket 统计
- 图表

P2：
- 更复杂的分布距离
- 多 horizon 对比
- 自动生成更完整 markdown report

====================
十四、我希望你最终给我的输出
====================

请完成后汇报：
1. 新增/修改了哪些文件
2. 如何运行后端分析脚本
3. 如何启动前端
4. 输出文件路径
5. 当前 subset_predictions 实际 schema
6. 当前是否已支持 probability metrics
7. 测试结果
8. 一个最小使用示例
```

我再给你一个判断标准：
这个功能做完后，你下一步就不需要凭感觉说“弱模态有没有条件性价值”了，而是可以直接看四个数：

* `complementarity_rate`
* `rescue_rate_given_complementary`
* `unused_complementary_rate`
* `negative_transfer_rate`

如果你最后发现：

* `complementarity_rate` 很低
  → 弱模态本身确实很难提供补充信息

* `complementarity_rate` 不低，但 `rescue_rate` 很低
  → 弱模态有信息，问题在 fusion 没利用上

* `rescue_rate` 有，但 `negative_transfer_rate` 也很高
  → 很适合做 safe fusion / sample-wise gate

* 互补样本集中在特定 bucket
  → 你就真正找到了 `conditional utility`

这会直接帮助你决定后面到底做：

* strong path
* safe fusion
* degraded robustness
* 还是重新做弱模态利用机制。

[1]: https://www.gradio.app/docs/gradio/blocks?utm_source=chatgpt.com "Blocks"
