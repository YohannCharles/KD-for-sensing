可以。下面这份可以**直接发给 Codex**，让它按这个方案实现一个 Gradio 版多模态时序样本浏览器。

Gradio 适合这个任务，因为 `Blocks` 提供比 `Interface` 更灵活的布局和事件流控制；`Slider` 可以做样本时间轴；`Image` 展示原始/处理后图像；`Plot` 支持 matplotlib、plotly 等图；`Dataframe` 和 `JSON` 适合展示置信度、质量分数、标签等结构化信息；自动播放可用 `Timer.tick` 周期触发。([Gradio][1])

---

# 给 Codex 的完整实现方案

```text
请帮我在当前项目中实现一个 Gradio 可交互网页，用于浏览通信多模态融合数据，文件名建议为：

tools/visualization/gradio_multimodal_viewer.py

目标：
实现一个“多模态时序样本浏览器”。用户可以通过一个滑块按照时间顺序浏览样本，每个样本同时展示：
1. 原始 image / lidar / radar / gps / mmWave
2. 处理后的 image / lidar / radar / gps / mmWave
3. 样本标签：current beam、future beams
4. 模型预测：fusion pred、top-k、是否正确
5. 每个单模态的置信度
6. 每个模态的质量分数
7. 可选：gate weight、单模态 top-k、loss 等调试信息

实现方式：
使用 Python + Gradio + Plotly + Pandas + PIL。
第一版不要使用 React / FastAPI，直接用 Gradio 本地启动即可。

==================================================
一、推荐目录结构
==================================================

请新增：

tools/visualization/
├── gradio_multimodal_viewer.py
├── viewer_utils.py
└── README.md

如果项目里已经有 tools 或 visualization 目录，请沿用已有结构。

其中：

1. gradio_multimodal_viewer.py
   - Gradio UI 主入口
   - 负责创建 Blocks 页面、绑定事件、启动 demo

2. viewer_utils.py
   - 负责读取 samples.json
   - 负责安全加载图片
   - 负责生成 GPS 轨迹图、mmWave beam power 图、置信度图、质量图
   - 负责样本过滤、索引映射、字段兼容

3. README.md
   - 写清楚如何准备 samples.json
   - 写清楚如何运行
   - 写清楚支持哪些字段

==================================================
二、运行方式
==================================================

命令行支持：

python tools/visualization/gradio_multimodal_viewer.py \
  --manifest path/to/samples.json \
  --host 0.0.0.0 \
  --port 7860

参数要求：

--manifest:
  必选或默认 data/visualization/samples.json

--host:
  默认 127.0.0.1

--port:
  默认 7860

--share:
  可选，默认 False

--debug:
  可选，默认 False

==================================================
三、依赖
==================================================

请在 README 中说明需要：

gradio
pandas
plotly
pillow
numpy

如果项目已有 requirements.txt，可以不要强行改主 requirements。
也可以新增：

tools/visualization/requirements_viewer.txt

内容：

gradio
pandas
plotly
pillow
numpy

==================================================
四、samples.json 数据格式
==================================================

第一版使用一个总的 samples.json。每个样本是一个 dict，所有样本按时间顺序排列。

示例：

[
  {
    "sample_id": "000001",
    "scene_id": "scene9",
    "split": "val",
    "sequence_id": "seq_001",
    "time_index": 0,
    "timestamp": "000001",

    "raw": {
      "image": "data/raw/image/000001.jpg",
      "lidar": "data/raw/lidar/000001_bev.png",
      "radar": "data/raw/radar/000001_heatmap.png",
      "gps": "data/raw/gps/000001.json",
      "mmwave": "data/raw/mmwave/000001.json"
    },

    "processed": {
      "image": "data/processed/image/000001.jpg",
      "lidar": "data/processed/lidar/000001_bev.png",
      "radar": "data/processed/radar/000001_heatmap.png",
      "gps": "data/processed/gps/000001.json",
      "mmwave": "data/processed/mmwave/000001.png"
    },

    "label": {
      "current_beam": 12,
      "future_beams": [15, 16, 16, 17]
    },

    "prediction": {
      "fusion_pred": 16,
      "fusion_confidence": 0.93,
      "topk": [16, 15, 17],
      "correct": true
    },

    "confidence": {
      "image": 0.81,
      "lidar": 0.74,
      "radar": 0.42,
      "gps": 0.57,
      "mmwave": 0.90,
      "fusion": 0.93
    },

    "quality": {
      "image": 0.68,
      "lidar": 0.83,
      "radar": 0.41,
      "gps": 0.79,
      "mmwave": 0.92
    },

    "gate": {
      "image": 0.30,
      "lidar": 0.25,
      "radar": 0.10,
      "gps": 0.12,
      "mmwave": 0.23
    },

    "extra": {
      "loss": 1.24,
      "note": "optional debug info"
    }
  }
]

字段要求：

1. 必须字段：
   - sample_id
   - raw
   - processed
   - label

2. 推荐字段：
   - scene_id
   - split
   - sequence_id
   - time_index
   - prediction
   - confidence
   - quality
   - gate

3. 程序必须允许部分字段缺失。
   比如某个样本没有 processed.mmwave，不要崩溃，而是显示空图或提示 Missing。

==================================================
五、GPS 和 mmWave 的 JSON 格式
==================================================

GPS 文件如果是 JSON，支持两种格式。

格式 A：

{
  "x": [0.0, 0.2, 0.5, 0.8],
  "y": [0.0, 0.1, 0.1, 0.2]
}

格式 B：

[
  {"x": 0.0, "y": 0.0},
  {"x": 0.2, "y": 0.1},
  {"x": 0.5, "y": 0.1}
]

mmWave 文件如果是 JSON，也支持两种格式。

格式 A，当前 beam power：

{
  "beam_power": [0.1, 0.2, 0.9, 0.3]
}

格式 B，时序 beam power：

{
  "beam_power_seq": [
    [0.1, 0.2, 0.9, 0.3],
    [0.2, 0.1, 0.8, 0.4],
    [0.1, 0.3, 0.7, 0.5]
  ]
}

如果 mmWave 是 PNG，则直接用 gr.Image 展示。
如果是 JSON，则用 Plotly 画图：
- beam_power: bar chart
- beam_power_seq: heatmap

==================================================
六、页面布局
==================================================

使用 gr.Blocks。

整体布局：

顶部控制区：
- Markdown 标题
- Scene 下拉选择器
- Split 下拉选择器
- Show Mode 下拉选择器
  - all
  - correct only
  - wrong only
  - low quality only
- Sample Slider
- 当前样本编号文本
- 上一帧按钮
- 下一帧按钮
- 播放 Checkbox
- 播放速度 Dropdown：1, 2, 5
- Timer 用于自动播放

主体区域建议用 gr.Tabs：

Tab 1: Overview
  - 左列：Raw Modalities
  - 中列：Processed Modalities
  - 右列：Info / Confidence / Quality

Tab 2: Raw Modalities
  - 更大尺寸展示 raw image / lidar / radar / gps / mmWave

Tab 3: Processed Modalities
  - 更大尺寸展示 processed image / lidar / radar / gps / mmWave

Tab 4: Diagnostics
  - confidence plot
  - quality plot
  - gate plot
  - label json
  - prediction json
  - extra json

第一版可以先实现 Tab 1，Tabs 2-4 可以作为扩展，但最好一起把结构写好。

==================================================
七、组件设计
==================================================

控制组件：

scene_dropdown = gr.Dropdown(...)
split_dropdown = gr.Dropdown(...)
show_mode_dropdown = gr.Dropdown(...)
sample_slider = gr.Slider(...)
sample_text = gr.Markdown(...)
prev_btn = gr.Button("上一帧")
next_btn = gr.Button("下一帧")
play_checkbox = gr.Checkbox(label="自动播放", value=False)
speed_dropdown = gr.Dropdown(choices=[1, 2, 5], value=1)
timer = gr.Timer(value=1.0, active=True)

Raw 输出组件：

raw_image = gr.Image(label="Raw Image", type="pil")
raw_lidar = gr.Image(label="Raw LiDAR / BEV", type="pil")
raw_radar = gr.Image(label="Raw Radar", type="pil")
raw_gps_plot = gr.Plot(label="Raw GPS Trajectory")
raw_mmwave_plot = gr.Plot(label="Raw mmWave")

Processed 输出组件：

proc_image = gr.Image(label="Processed Image", type="pil")
proc_lidar = gr.Image(label="Processed LiDAR", type="pil")
proc_radar = gr.Image(label="Processed Radar", type="pil")
proc_gps_plot = gr.Plot(label="Processed GPS")
proc_mmwave_plot_or_image = gr.Plot 或 gr.Image

诊断输出组件：

info_json = gr.JSON(label="Sample / Label / Prediction")
confidence_plot = gr.Plot(label="Single-Modality Confidence")
quality_plot = gr.Plot(label="Modality Quality")
gate_plot = gr.Plot(label="Gate Weight")
confidence_df = gr.Dataframe(label="Confidence Table")
quality_df = gr.Dataframe(label="Quality Table")
gate_df = gr.Dataframe(label="Gate Table")

注意：
如果 processed.mmwave 有时是图片、有时是 JSON，第一版可以统一用 gr.Plot：
- 如果是图片，就返回一个 plotly image figure
- 如果是 JSON，就画 bar 或 heatmap
或者简单一点，拆成两个组件：
- proc_mmwave_image
- proc_mmwave_plot
根据内容缺失时返回 None。

==================================================
八、核心函数设计
==================================================

请实现以下函数。

1. load_manifest(manifest_path: str) -> list[dict]

功能：
- 读取 samples.json
- 检查是否是 list
- 按 scene_id、sequence_id、time_index 排序
- 如果没有 time_index，则按原始顺序
- 给每个样本增加内部字段 "_global_index"

2. get_available_scenes(samples) -> list[str]

返回所有 scene_id。
如果没有 scene_id，返回 ["all"]。

3. get_available_splits(samples) -> list[str]

返回所有 split。
如果没有 split，返回 ["all"]。

4. filter_samples(samples, scene, split, show_mode) -> list[dict]

scene:
- all 表示不过滤
- 否则 sample["scene_id"] == scene

split:
- all 表示不过滤
- 否则 sample["split"] == split

show_mode:
- all: 不过滤
- correct only: prediction.correct == true
- wrong only: prediction.correct == false
- low quality only: 至少一个模态 quality < 0.4，或者平均 quality < 0.5

5. safe_get(d: dict, path: str, default=None)

例如：
safe_get(sample, "raw.image")
safe_get(sample, "prediction.fusion_pred")

6. load_image_safe(path: str)

要求：
- path 缺失返回 None
- 文件不存在返回 None
- 能打开则返回 PIL.Image
- 不要因为文件坏掉导致整个页面崩溃

7. load_json_safe(path: str)

要求：
- path 缺失返回 None
- 文件不存在返回 None
- JSON 解析失败返回 None

8. make_empty_figure(title: str)

返回一个空的 plotly figure，中央写 Missing / Not Available。

9. make_gps_figure(gps_data, title)

输入可以是：
- JSON 路径
- dict
- list

输出：
- plotly line chart
- x 轴为 x/east/lon
- y 轴为 y/north/lat
- 当前点用 marker 标出
- 如果数据缺失，返回 empty figure

10. make_mmwave_figure(mmwave_data, title)

输入可以是：
- JSON 路径
- dict
- list

如果有 beam_power：
- 画 bar chart
- x: beam index
- y: power

如果有 beam_power_seq：
- 画 heatmap
- x: beam index
- y: time index
- color: power

如果缺失：
- 返回 empty figure

11. make_score_bar(score_dict, title, y_range=[0, 1])

用于 confidence / quality / gate。
要求：
- plotly bar
- modality 为 x
- score 为 y
- 范围 0 到 1
- 缺失时返回 empty figure

12. dict_to_dataframe(score_dict, value_name)

将 confidence / quality / gate 转成 pandas.DataFrame：
columns = ["modality", value_name]

13. build_info(sample)

返回一个 dict，只包含最关键的信息：

{
  "sample_id": ...,
  "scene_id": ...,
  "split": ...,
  "sequence_id": ...,
  "time_index": ...,
  "timestamp": ...,
  "label": ...,
  "prediction": ...,
  "extra": ...
}

14. render_sample(view_index, scene, split, show_mode)

这是最核心函数。

输入：
- 当前 slider index
- scene
- split
- show_mode

流程：
- filtered = filter_samples(...)
- view_index 截断到合法范围
- sample = filtered[view_index]
- 读取 raw image/lidar/radar
- raw gps 画 plot
- raw mmwave 画 plot
- 读取 processed image/lidar/radar
- processed gps 画 plot
- processed mmwave 画 plot
- 构建 info_json
- 构建 confidence plot / table
- 构建 quality plot / table
- 构建 gate plot / table
- 返回所有 outputs

必须返回：
- raw_image
- raw_lidar
- raw_radar
- raw_gps_plot
- raw_mmwave_plot
- proc_image
- proc_lidar
- proc_radar
- proc_gps_plot
- proc_mmwave_plot
- info_json
- confidence_plot
- quality_plot
- gate_plot
- confidence_df
- quality_df
- gate_df
- sample_text

15. update_filter(scene, split, show_mode)

当 scene/split/show_mode 改变时调用。

要求：
- 重新计算 filtered_samples 长度
- 更新 slider maximum
- slider value 置 0
- 渲染第 0 个样本

注意：
Gradio 里需要用 gr.update(maximum=..., value=0) 更新 slider。

16. go_prev(current_index, scene, split, show_mode)

返回新 index：
max(0, current_index - 1)

然后触发 render_sample。

17. go_next(current_index, scene, split, show_mode)

返回新 index：
min(num_filtered - 1, current_index + 1)

然后触发 render_sample。

18. autoplay_step(current_index, play, speed, scene, split, show_mode)

Timer 每秒触发一次。

如果 play == False：
- 返回当前 index，不改变页面，或者直接返回 gr.update()

如果 play == True：
- new_index = current_index + int(speed)
- 如果超过最后一个样本，则回到 0，形成循环播放
- 更新 slider
- 渲染新样本

==================================================
九、事件绑定
==================================================

需要绑定：

1. demo.load
- 初始加载第一个样本

2. sample_slider.change
- 调用 render_sample

3. scene_dropdown.change
- 调用 update_filter

4. split_dropdown.change
- 调用 update_filter

5. show_mode_dropdown.change
- 调用 update_filter

6. prev_btn.click
- 先更新 slider index
- 再 render_sample

7. next_btn.click
- 先更新 slider index
- 再 render_sample

8. timer.tick
- 调用 autoplay_step
- 如果 play_checkbox 为 True，就自动前进

==================================================
十、布局细节
==================================================

建议页面标题：

# Multimodal Fusion Temporal Sample Viewer

副标题：

用于按时间顺序浏览 image / lidar / radar / gps / mmWave 多模态样本、处理结果、标签、置信度和质量分数。

Overview Tab 布局：

with gr.Row():
    with gr.Column(scale=2):
        Raw Modalities
    with gr.Column(scale=2):
        Processed Modalities
    with gr.Column(scale=1):
        Diagnostics

Raw Modalities 中：
- raw_image
- raw_lidar
- raw_radar
- raw_gps_plot
- raw_mmwave_plot

Processed Modalities 中：
- proc_image
- proc_lidar
- proc_radar
- proc_gps_plot
- proc_mmwave_plot

Diagnostics 中：
- info_json
- confidence_plot
- quality_plot
- gate_plot

如果页面太长，可以用 Accordion：

gr.Accordion("Raw Modalities", open=True)
gr.Accordion("Processed Modalities", open=True)
gr.Accordion("Diagnostics", open=True)

==================================================
十一、容错要求
==================================================

必须做到：

1. 某个路径不存在，不崩溃
2. 某个字段不存在，不崩溃
3. 某个 JSON 解析失败，不崩溃
4. 某个模态缺失，不崩溃
5. filtered_samples 为空时，页面显示：
   - No samples found
   - 所有图为空
6. confidence / quality / gate 缺失时显示空图
7. low quality only 如果没有 quality 字段，则不要报错

==================================================
十二、可选增强功能
==================================================

第一版完成后，可以继续加：

1. 错误样本快速跳转
   - “Next Wrong” 按钮
   - 跳到下一个 prediction.correct == false 的样本

2. 低质量样本快速跳转
   - “Next Low Quality” 按钮

3. Top-k 展示
   - 用 JSON 或表格显示每个模态 top-k
   - 如果样本里有 modality_predictions 字段，则显示

4. Gate weight
   - 如果 sample["gate"] 存在，则显示 gate bar
   - 如果不存在，显示 empty figure

5. 导出当前样本截图
   - 第一版可以不做

6. 样本备注
   - gr.Textbox 允许用户给当前样本写 note
   - 保存到 local notes.json
   - 第一版可以不做

==================================================
十三、与模型输出的对接建议
==================================================

如果训练代码可以导出预测结果，请新增一个脚本：

tools/visualization/export_viewer_manifest.py

它的任务是把原始数据路径、处理后数据路径、标签、预测、置信度、质量分数合并成 samples.json。

输入可以来自：
- dataset metadata
- model prediction npy/json/pkl
- quality score json
- gate weight json

输出：
- samples.json

第一版 gradio viewer 不需要直接调用模型，只读取已经导出的 samples.json。
这样更稳定，也更容易调试。

==================================================
十四、README 需要写清楚
==================================================

README 至少包括：

1. 功能简介
2. 安装依赖
3. samples.json 格式
4. 运行命令
5. 常见问题

示例：

运行：

python tools/visualization/gradio_multimodal_viewer.py \
  --manifest data/visualization/samples.json \
  --port 7860

浏览器打开：

http://127.0.0.1:7860

==================================================
十五、验收标准
==================================================

完成后需要满足：

1. 可以成功启动 Gradio 页面
2. 可以读取 samples.json
3. 可以通过 slider 切换样本
4. 上一帧 / 下一帧按钮可用
5. 自动播放可用
6. Scene / Split / Show Mode 过滤可用
7. 可以展示 raw image / lidar / radar
8. 可以展示 raw gps 轨迹图
9. 可以展示 raw mmWave beam power 图
10. 可以展示 processed image / lidar / radar
11. 可以展示 processed gps / mmWave
12. 可以展示 label / prediction JSON
13. 可以展示 confidence bar chart 和 table
14. 可以展示 quality bar chart 和 table
15. 如果 gate 字段存在，可以展示 gate bar chart 和 table
16. 任意模态缺失时不崩溃
17. 任意图片路径不存在时不崩溃
18. filtered_samples 为空时有明确提示

==================================================
十六、请优先实现的 MVP
==================================================

如果时间有限，优先实现这些：

1. gradio_multimodal_viewer.py
2. viewer_utils.py
3. 读取 samples.json
4. slider 浏览样本
5. prev / next
6. raw image / lidar / radar
7. raw gps plot
8. raw mmWave plot
9. processed image / lidar / radar
10. processed gps / mmWave plot
11. label / prediction JSON
12. confidence plot
13. quality plot
14. gate plot，如果字段存在

不要一开始实现截图导出、备注保存、复杂检索。
先保证浏览器稳定可用。

==================================================
十七、实现时的关键细节
==================================================

1. 不要一次性加载所有图片到内存。
   只在 render_sample 时加载当前样本。

2. samples.json 可以一次性读入内存。
   一般 metadata 不会太大。

3. 图片统一返回 PIL.Image 或 None。

4. GPS / mmWave 统一返回 plotly Figure。

5. 置信度、质量、gate 都用同一个 make_score_bar 函数。

6. 页面不需要很华丽，但布局必须清晰。

7. 代码里要有类型注解和必要注释。

8. 所有文件读取都要 try/except。

9. 对路径要支持相对路径。
   如果 samples.json 里是相对路径，应该相对于项目根目录或 manifest 所在目录解析。
   建议实现 resolve_path(path, manifest_dir, project_root=None)。

10. 不要修改训练主流程。
    这个 viewer 是独立工具。

==================================================
十八、最终交付内容
==================================================

请最终交付：

1. tools/visualization/gradio_multimodal_viewer.py
2. tools/visualization/viewer_utils.py
3. tools/visualization/README.md
4. 可选：tools/visualization/sample_manifest_example.json
5. 可选：tools/visualization/requirements_viewer.txt

完成后请说明如何运行，并给出一个最小 samples.json 示例。
```

---

# 我建议你让 Codex 先做的版本

你可以额外补一句给 Codex：

```text
先不要追求美观，先保证功能稳定。第一版只要能读取 samples.json，用 slider 浏览样本，展示五个原始模态、五个处理后模态、标签、预测、confidence、quality、gate 即可。所有缺失字段都要安全处理，不能因为某个模态缺失导致页面崩溃。
```

这个方案适合你当前阶段。后面你的 `quality-aware gate` 做出来后，只需要在 `samples.json` 里增加：

```json
"quality": {
  "image": 0.68,
  "lidar": 0.83,
  "radar": 0.41,
  "gps": 0.79,
  "mmwave": 0.92
},
"gate": {
  "image": 0.30,
  "lidar": 0.25,
  "radar": 0.10,
  "gps": 0.12,
  "mmwave": 0.23
}
```

网页就能直接把**模态质量**和**融合权重**一起可视化出来。
