- [x] 1.1 添加 soft beam label 生成 helper，覆盖 power/RSS 归一化、Gaussian fallback、circular distance 和无效输入。
- [x] 1.2 扩展 DeepSense6G/MMW Dataset 输出 `target_beam_distribution` 与可选 mask，并保留 hard `target_beam`。
- [x] 1.3 扩展 batch preparation，提供 `prepare_soft_beam_targets()`，处理 horizon 裁剪、num_classes/downsample 对齐和无 soft target fallback。
- [x] 2.1 扩展 focal loss/CE 路径，使 supervised task criterion 可消费 hard target 或 soft target。
- [x] 2.2 扩展 KD distiller 和 BatchStepRunner，使 no-KD/logits-KD/RKD 的 task loss 优先使用 soft target，hard labels 继续用于 metrics。
- [x] 2.3 确认 validation/evaluation pass 不消费 soft target，validation loss、top-k/DBA 和其它评估指标仍使用 hard labels。
- [x] 2.4 扩展 CRAF unimodal auxiliary 和 counterfactual beam loss，在 soft target 可用时与主 loss 对齐。
- [x] 3.1 更新默认配置/canonical objective recipe，暴露 `data.dataset.soft_beam_labels` 和 `loss.soft_targets` 参数。
- [x] 3.2 更新 MMW modal15 orchestration 默认覆盖，确保 source 训练启用 power/RSS 优先的 power-or-gaussian soft label，target 快速适应只使用 circular Gaussian soft label。
- [x] 4.1 添加或更新 focused tests，覆盖 soft distribution 生成、Dataset 输出、soft focal loss、batch preparation 和 validation/evaluation hard-label loss/metrics 保留。
- [x] 4.2 运行 `conda run -n kd_mm_beam pytest tests/test_craf_fusion.py tests/test_mmw_town10_preparation.py tests/test_evaluation_pass.py -q`。
- [x] 4.3 运行 `openspec validate add-soft-beam-labels --strict` 和 `openspec status --change add-soft-beam-labels`。
