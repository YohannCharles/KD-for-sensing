下面这版是**快速验证版，不是完整投稿系统**。目标是用 2–4 周验证这个想法值不值得继续做：

> **验证假设：在 DeepSense6G 31–34 跨场景波束预测中，把 beam label 拆成 coarse group + fine offset，并把特征拆成 shared coarse semantics + private scene refinement，再用小 adapter 适配 target，是否比普通 source-only / full fine-tuning / DANN-style 更合理。**

你可以直接把下面内容发给 Codex，让它按任务实现。

---

# 给 Codex 的实现任务书

## 0. 项目目标

请在现有多模态 beam prediction 框架上实现一个快速验证版本：

**HiST-Beam: Hierarchical Shared-private Target-adaptive Beam Prediction**

需要支持 DeepSense6G scenarios 31–34，完成以下实验：

1. Flat source-only baseline；
2. Hierarchical beam label baseline；
3. Shared-private decoupling；
4. Shared-private + lightweight target adapter；
5. Full fine-tuning baseline；
6. Few-shot target labels = 0 / 5 / 10 / 20 / 50。

优先保证代码清晰、可复现、可扩展，不追求一开始就最强 SOTA。

---

# 1. 数据划分

## 1.1 主实验：Leave-One-Scene-Out

使用 scenarios 31–34 做 4 折跨场景实验：

```text
Fold A: source = [31, 32, 33], target = 34
Fold B: source = [31, 32, 34], target = 33
Fold C: source = [31, 33, 34], target = 32
Fold D: source = [32, 33, 34], target = 31
```

每个 target scene 内部划分：

```text
target_adapt: 20%
target_test : 80%
```

target_adapt 用于无标签 / 少标签适应，target_test 只用于最终测试，严禁参与训练和阈值选择。

## 1.2 few-shot label budget

对 target_adapt 中样本采样：

```text
label_budget = [0, 5, 10, 20, 50]
seeds = [0, 1, 2]
```

快速验证阶段先跑 3 个 seed。结果稳定后再扩展到 5 个 seed。

采样策略：

优先使用 coarse-group stratified sampling。也就是尽量让少量 labeled target 样本覆盖不同 coarse beam group。如果某些 group 样本不足，则退化为随机采样。

---

# 2. 输入模态

快速验证阶段建议先用：

```text
camera + radar + GPS
```

LiDAR 先保留接口，但不作为第一阶段必须项。理由是 LiDAR 预处理最容易拖慢进度，而你的核心创新不是 LiDAR encoder。

## 2.1 Camera

输入：

```text
T_cam = 5 frames
image_size = 224 x 224
```

预处理：

```python
Resize(256)
CenterCrop(224)
Normalize(mean=[0.485,0.456,0.406], std=[0.229,0.224,0.225])
```

Encoder：

```text
MobileNetV3-Small 或 ResNet18
pretrained = True
output_dim = 128
```

快速版建议：

```text
ResNet18 pretrained，去掉最后 fc，接 Linear(512, 128)
```

时序聚合：

```text
对 5 帧分别编码，然后 mean pooling
```

不要一开始上 temporal transformer。

## 2.2 Radar

根据你现有框架决定：

如果 radar 已经是 range-angle map：

```text
input shape: [T_radar, C, H, W]
encoder: small CNN
output_dim = 128
```

Small CNN：

```python
Conv2d(C, 32, kernel_size=3, stride=2, padding=1)
BN + ReLU
Conv2d(32, 64, kernel_size=3, stride=2, padding=1)
BN + ReLU
Conv2d(64, 128, kernel_size=3, stride=2, padding=1)
BN + ReLU
AdaptiveAvgPool2d(1)
Linear(128, 128)
```

时序聚合：

```text
T_radar = 5
per-frame encode + mean pooling
```

## 2.3 GPS

输入：

```text
T_gps = 2
```

构造特征：

```python
[x1, y1, x2, y2, dx, dy, speed, heading, dist_to_origin, angle_to_origin]
```

其中：

```python
dx = x2 - x1
dy = y2 - y1
speed = sqrt(dx^2 + dy^2)
heading = atan2(dy, dx)
dist_to_origin = sqrt(x2^2 + y2^2)
angle_to_origin = atan2(y2, x2)
```

如果 DeepSense 中是经纬度，先转局部平面坐标。快速版可以以每个 scene 第一个 GPS 点为局部原点，但注意归一化统计量只能由 source train 计算。

GPS encoder：

```python
MLP(
  Linear(10, 64),
  LayerNorm(64),
  GELU,
  Dropout(0.1),
  Linear(64, 128),
  LayerNorm(128),
  GELU
)
```

## 2.4 模态融合

不要用大 transformer。使用 tiny token fusion。

每个模态输出：

```text
z_camera ∈ R^128
z_radar  ∈ R^128
z_gps    ∈ R^128
```

投影到统一维度：

```text
d_model = 256
```

```python
z_m = Linear(128, 256)(z_m)
```

加 modality embedding：

```text
camera_embed, radar_embed, gps_embed
```

然后输入 2-layer Transformer Encoder：

```yaml
fusion:
  type: tiny_transformer
  d_model: 256
  n_heads: 4
  num_layers: 2
  dim_feedforward: 512
  dropout: 0.1
```

输出：

```text
h = mean_pool(tokens) ∈ R^256
```

---

# 3. 层次化 beam label

DeepSense challenge 使用 64-beam codebook。设：

```text
B = 64
K0 = 8
G = B / K0 = 8
```

对原始 beam index：

```python
g = beam_index // K0       # coarse group, 0~7
delta = beam_index % K0    # fine offset, 0~7
```

需要支持消融：

```text
K0 in [2, 4, 8, 16]
```

但快速主实验先用：

```text
K0 = 8
```

---

# 4. 模型结构

## 4.1 Shared-private split

融合特征：

```text
h ∈ R^256
```

Shared branch：

```python
Ec:
  Linear(256, 256)
  LayerNorm(256)
  GELU
  Dropout(0.1)
  Linear(256, 128)
```

输出：

```text
c ∈ R^128
```

Private branch：

```python
Es:
  Linear(256, 128)
  LayerNorm(128)
  GELU
  Dropout(0.2)
  Linear(128, 64)
```

输出：

```text
s ∈ R^64
```

故意让 private branch 更窄，避免 private branch 偷学全部信息。

## 4.2 Prediction heads

Coarse head 只看 shared representation：

```python
coarse_head:
  Linear(128, 128)
  GELU
  Dropout(0.1)
  Linear(128, G)
```

Fine head 看 shared + private：

```python
fine_head:
  input = concat(c, s)  # 128 + 64 = 192
  Linear(192, 256)
  GELU
  Dropout(0.1)
  Linear(256, G * K0)
```

输出 reshape：

```python
fine_logits = fine_logits.view(batch_size, G, K0)
```

最终 beam log probability：

```python
log_p_g = log_softmax(coarse_logits, dim=-1)      # [B, G]
log_p_d = log_softmax(fine_logits, dim=-1)        # [B, G, K0]
log_p_b = log_p_g[:, :, None] + log_p_d           # [B, G, K0]
log_p_b = log_p_b.reshape(batch_size, 64)
```

预测：

```python
pred_beam = argmax(log_p_b)
```

---

# 5. Loss 设计

快速验证阶段不要堆太多 loss。主线只实现 5 个：

```text
L = L_hier + λ_flat L_flat + λ_orth L_orth + λ_scene_c L_scene_c + λ_scene_s L_scene_s
```

## 5.1 Hierarchical loss

```python
L_coarse = CE(coarse_logits, g_true)
L_fine = CE(fine_logits[range(B), g_true, :], delta_true)
L_hier = L_coarse + lambda_delta * L_fine
```

参数：

```yaml
lambda_delta: 0.7
```

## 5.2 Flat auxiliary loss

从 `log_p_b` 得到 beam-level prediction：

```python
L_flat = NLLLoss(log_p_b, beam_true)
```

参数：

```yaml
lambda_flat: 0.3
```

作用：防止模型只优化 coarse group，不关心最终 beam Top-1。

## 5.3 Orthogonality loss

```python
C = normalize(c - c.mean(dim=0), dim=1)
S = normalize(s - s.mean(dim=0), dim=1)
L_orth = || C.T @ S / batch_size ||_F^2
```

参数：

```yaml
lambda_orth: 0.02
```

## 5.4 Shared branch scene confusion

为了让 shared representation 少包含 scene 信息，给 `c` 接一个 scene classifier，并使用 GRL。

```python
scene_logits_c = scene_classifier_c(GRL(c))
L_scene_c = CE(scene_logits_c, scene_id)
```

注意：这是对 shared branch 的局部 scene confusion，不是对整个表示做 DANN。

参数：

```yaml
lambda_scene_c: 0.05
grl_lambda: 1.0
```

## 5.5 Private branch scene preservation

让 private branch 保留 scene-specific 信息：

```python
scene_logits_s = scene_classifier_s(s.detach_or_not)
L_scene_s = CE(scene_logits_s, scene_id)
```

这里不要 detach `s`，需要让 private branch 学到 scene 信息。

参数：

```yaml
lambda_scene_s: 0.1
```

## 5.6 可选 angular smoothing loss

快速版可以先不实现。如果实现：

```python
q_j ∝ exp(- circular_distance(j, beam_true)^2 / (2 * sigma^2))
L_ang = - sum_j q_j log p_j
```

参数：

```yaml
lambda_ang: 0.05
sigma: 1.5
```

第一阶段建议先关掉：

```yaml
use_angular_loss: false
```

---

# 6. Target adapter

## 6.1 冻结策略

target adaptation 阶段冻结：

```text
camera_encoder
radar_encoder
gps_encoder
fusion_transformer
Ec
coarse_head
Es 原始参数
```

训练：

```text
private_adapter
fine_head 最后一层
LayerNorm affine 参数，可选
target prototype bank
```

## 6.2 Adapter 形式

对 private representation 加 bottleneck residual adapter：

```python
s_adapt = s + U(GELU(V(s)))
```

参数：

```yaml
private_dim: 64
adapter_rank: 16
```

实现：

```python
class BottleneckAdapter(nn.Module):
    def __init__(self, dim=64, rank=16):
        super().__init__()
        self.down = nn.Linear(dim, rank)
        self.up = nn.Linear(rank, dim)
        self.act = nn.GELU()

        nn.init.zeros_(self.up.weight)
        nn.init.zeros_(self.up.bias)

    def forward(self, s):
        return s + self.up(self.act(self.down(s)))
```

初始化 `up` 为 0，保证 adaptation 初始时等价于 source model。

## 6.3 Few-shot supervised adaptation

有标签 target 样本时，优化：

```text
L_target_sup = L_hier + λ_flat L_flat
```

参数：

```yaml
target_adapt:
  epochs: 30
  batch_size: 64
  optimizer: AdamW
  lr: 0.001
  weight_decay: 0.0001
  early_stop_patience: 8
```

注意 label_budget 很小时，允许重复采样 labeled target batch。

## 6.4 Unlabeled target adaptation

label_budget = 0 时使用 entropy minimization + prototype consistency。

### Source prototypes

source pretraining 结束后，保存 coarse prototypes：

```python
mu_g = mean(normalize(c_i)) for source samples with coarse label g
rho_g = mean(normalize(s_i)) for source samples with coarse label g
```

保存：

```text
prototypes_source.pt
  mu_c: [G, 128]
  rho_s: [G, 64]
```

### Target soft assignment

对 target 样本：

```python
alpha_g = softmax(cosine(c, mu_g) / tau)
```

参数：

```yaml
proto_tau: 0.1
confidence_threshold: 0.7
```

### Prototype loss

```python
g_hat = argmax(alpha)
conf = max(alpha)

if conf > threshold:
    L_proto_s = || normalize(s_adapt) - rho_s[g_hat] ||_2^2
else:
    ignore
```

再加 entropy loss：

```python
p_beam = exp(log_p_b)
L_ent = - sum_j p_beam_j log p_beam_j
```

无标签 loss：

```text
L_unsup = λ_ent L_ent + λ_proto L_proto_s
```

参数：

```yaml
lambda_ent: 0.01
lambda_proto: 0.1
```

## 6.5 Semi-supervised adaptation

当 label_budget > 0 且还有 unlabeled target 时：

```text
L_target = L_sup_labeled + λ_unsup L_unsup_unlabeled
```

参数：

```yaml
lambda_unsup: 0.5
```

---

# 7. 需要实现的 variants

请实现统一入口，通过 config 控制。

## Variant V0: Flat source-only

```yaml
use_hierarchical: false
use_shared_private: false
use_decoupling_loss: false
use_target_adapter: false
```

模型：

```text
h -> Linear -> 64-class beam classifier
```

## Variant V1: Hierarchical source-only

```yaml
use_hierarchical: true
use_shared_private: false
```

可以直接用：

```text
h -> coarse_head
h -> fine_head
```

没有 shared/private。

## Variant V2: Shared-private no decoupling

```yaml
use_hierarchical: true
use_shared_private: true
lambda_orth: 0
lambda_scene_c: 0
lambda_scene_s: 0
```

## Variant V3: Shared-private decoupling

```yaml
use_hierarchical: true
use_shared_private: true
lambda_orth: 0.02
lambda_scene_c: 0.05
lambda_scene_s: 0.1
```

## Variant V4: Adapter-only

先训练 V3 source model，然后 target adaptation：

```yaml
use_target_adapter: true
use_proto_align: false
```

## Variant V5: Adapter + prototype

```yaml
use_target_adapter: true
use_proto_align: true
```

这是你的 full method 快速版。

## Variant V6: Full fine-tuning baseline

加载 V3 source model，然后在 target labeled samples 上更新全部参数：

```yaml
freeze_backbone: false
lr: 0.00003
epochs: 30
```

---

# 8. 训练参数

## 8.1 Source training

```yaml
source_train:
  epochs: 60
  batch_size: 128
  optimizer: AdamW
  lr: 0.0003
  weight_decay: 0.0001
  scheduler: cosine
  warmup_epochs: 5
  grad_clip: 5.0
  amp: true
  early_stop_patience: 12
```

如果显存不够：

```yaml
batch_size: 64
gradient_accumulation_steps: 2
```

## 8.2 Target adaptation

```yaml
target_adapt:
  epochs: 30
  batch_size: 64
  optimizer: AdamW
  lr_adapter: 0.001
  lr_fine_head: 0.0003
  weight_decay: 0.0001
  amp: true
  early_stop_patience: 8
```

## 8.3 随机种子

```yaml
seeds: [0, 1, 2]
```

所有实验必须保存：

```text
config.yaml
metrics.json
best_checkpoint.pt
test_predictions.csv
```

---

# 9. 评价指标

必须实现：

```text
Top-1 accuracy
Top-3 accuracy
Top-5 accuracy
coarse group accuracy
fine offset accuracy
trainable parameter ratio
adaptation time
```

如果数据中有每个 beam 的 power vector，则实现：

```text
normalized received power = P[pred_beam] / P[best_beam]
beam power loss dB = 10 * log10(P[best_beam] / P[pred_beam])
```

如果没有 power vector，先跳过 power 指标，不要伪造。

## 9.1 Top-k

```python
topk_acc = beam_true in topk(pred_logits, k)
```

## 9.2 Coarse accuracy

```python
pred_group = pred_beam // K0
true_group = true_beam // K0
coarse_acc = mean(pred_group == true_group)
```

这个指标非常重要，用来验证 shared coarse semantics 是否真的有效。

---

# 10. 结果表格

快速验证至少输出 3 张表。

## 表 1：LOSO source-only 验证

| Method            | 31→target | 32→target | 33→target | 34→target | Avg Top-1 | Avg Top-3 | Avg Coarse Acc |
| ----------------- | --------: | --------: | --------: | --------: | --------: | --------: | -------------: |
| V0 Flat           |           |           |           |           |           |           |                |
| V1 Hierarchical   |           |           |           |           |           |           |                |
| V2 Shared-private |           |           |           |           |           |           |                |
| V3 Decoupled      |           |           |           |           |           |           |                |

想验证：

```text
V1 > V0：层次化 beam label 有用
V3 > V2：解耦约束有用
coarse acc 提升明显：shared coarse semantics 有价值
```

## 表 2：Few-shot target adaptation

| Target labels | Source-only V3 | Full FT | Adapter-only | Adapter+Proto |
| ------------: | -------------: | ------: | -----------: | ------------: |
|             0 |                |         |              |               |
|             5 |                |         |              |               |
|            10 |                |         |              |               |
|            20 |                |         |              |               |
|            50 |                |         |              |               |

想验证：

```text
0 label: Adapter+Proto > Source-only
5/10 labels: Adapter+Proto 明显优于 Adapter-only
20/50 labels: Adapter+Proto 接近 Full FT，但训练参数远少于 Full FT
```

## 表 3：效率

| Method        | Trainable params | Trainable ratio | Adapt time / epoch | Total adapt time | Top-1 |
| ------------- | ---------------: | --------------: | -----------------: | ---------------: | ----: |
| Full FT       |                  |            100% |                    |                  |       |
| Adapter-only  |                  |             <2% |                    |                  |       |
| Adapter+Proto |                  |             <2% |                    |                  |       |

想验证：

```text
性能接近 Full FT，但 trainable params 只有 0.5%–2%
```

---

# 11. 代码结构建议

```text
project/
  configs/
    hist_beam_base.yaml
    exp_v0_flat.yaml
    exp_v1_hier.yaml
    exp_v2_sp.yaml
    exp_v3_sp_decouple.yaml
    exp_v4_adapter.yaml
    exp_v5_adapter_proto.yaml

  datasets/
    deepsense_dataset.py
    splits.py
    transforms.py

  models/
    encoders/
      camera_encoder.py
      radar_encoder.py
      gps_encoder.py
      lidar_encoder.py
    fusion.py
    hist_beam.py
    adapters.py
    grl.py

  losses/
    hierarchical_loss.py
    decoupling_loss.py
    prototype_loss.py

  train_source.py
  adapt_target.py
  evaluate.py
  run_loso.py

  utils/
    metrics.py
    prototypes.py
    seed.py
    logger.py
    checkpoint.py
```

---

# 12. 关键实现细节

## 12.1 Dataset 输出格式

`__getitem__` 返回：

```python
{
    "camera": Tensor[T_cam, 3, 224, 224],
    "radar": Tensor[T_radar, C, H, W],
    "gps": Tensor[10],
    "beam": LongTensor[],
    "scene": LongTensor[],
    "sample_id": str,
    "beam_power": Optional[Tensor[64]]
}
```

如果某个模态缺失，返回 None，并在 collate_fn 中处理。快速版可以先要求 camera/radar/GPS 都存在。

## 12.2 Model forward 输出

```python
outputs = {
    "coarse_logits": Tensor[B, G],
    "fine_logits": Tensor[B, G, K0],
    "beam_log_probs": Tensor[B, 64],
    "c": Tensor[B, 128],
    "s": Tensor[B, 64],
    "s_adapt": Optional[Tensor[B, 64]],
    "scene_logits_c": Optional[Tensor[B, num_source_scenes]],
    "scene_logits_s": Optional[Tensor[B, num_source_scenes]]
}
```

## 12.3 保存 prototypes

source training 结束后，对 source train set 跑一遍 forward，保存：

```python
{
    "mu_c": Tensor[G, 128],
    "rho_s": Tensor[G, 64],
    "count": Tensor[G]
}
```

---

# 13. 一份基础 YAML 配置

```yaml
project_name: hist_beam_quick_verify

data:
  root: /path/to/DeepSense6G
  scenarios: [31, 32, 33, 34]
  target_scene: 34
  source_scenes: [31, 32, 33]
  target_adapt_ratio: 0.2
  target_test_ratio: 0.8
  modalities: [camera, radar, gps]
  num_beams: 64
  num_workers: 8

label:
  hierarchical: true
  group_size: 8
  num_groups: 8

model:
  camera_encoder: resnet18
  camera_pretrained: true
  radar_encoder: small_cnn
  gps_encoder: mlp
  modality_dim: 128
  d_model: 256
  fusion_layers: 2
  fusion_heads: 4
  fusion_ffn_dim: 512
  fusion_dropout: 0.1
  shared_dim: 128
  private_dim: 64
  private_dropout: 0.2
  adapter_rank: 16

loss:
  lambda_delta: 0.7
  lambda_flat: 0.3
  lambda_orth: 0.02
  lambda_scene_c: 0.05
  lambda_scene_s: 0.1
  use_angular_loss: false
  lambda_ang: 0.05
  angular_sigma: 1.5

source_train:
  epochs: 60
  batch_size: 128
  lr: 0.0003
  weight_decay: 0.0001
  optimizer: adamw
  scheduler: cosine
  warmup_epochs: 5
  grad_clip: 5.0
  amp: true
  early_stop_patience: 12

target_adapt:
  label_budget: 10
  epochs: 30
  batch_size: 64
  lr_adapter: 0.001
  lr_fine_head: 0.0003
  lr_full_ft: 0.00003
  weight_decay: 0.0001
  confidence_threshold: 0.7
  proto_tau: 0.1
  lambda_ent: 0.01
  lambda_proto: 0.1
  lambda_unsup: 0.5
  amp: true
  early_stop_patience: 8

experiment:
  seeds: [0, 1, 2]
  save_predictions: true
  save_prototypes: true
```

---

# 14. 运行命令设计

Codex 需要实现以下命令。

## 14.1 训练 source model

```bash
python train_source.py \
  --config configs/exp_v3_sp_decouple.yaml \
  --source_scenes 31 32 33 \
  --target_scene 34 \
  --seed 0
```

输出：

```text
runs/v3_sp_decouple/src_31_32_33_tgt_34/seed0/
  best_source.pt
  prototypes_source.pt
  metrics_source_val.json
  config.yaml
```

## 14.2 测试 source-only

```bash
python evaluate.py \
  --checkpoint runs/.../best_source.pt \
  --split target_test \
  --target_scene 34
```

## 14.3 target adaptation

```bash
python adapt_target.py \
  --checkpoint runs/.../best_source.pt \
  --prototypes runs/.../prototypes_source.pt \
  --target_scene 34 \
  --label_budget 10 \
  --method adapter_proto \
  --seed 0
```

输出：

```text
best_adapted.pt
metrics_target_test.json
test_predictions.csv
adapt_log.json
```

## 14.4 一键 LOSO

```bash
python run_loso.py \
  --config configs/exp_v5_adapter_proto.yaml \
  --label_budgets 0 5 10 20 50 \
  --seeds 0 1 2
```

---

# 15. 快速验证的优先级

不要一开始全部做。按下面顺序推进。

## P0：先保证数据和指标正确

必须先完成：

```text
LOSO split
beam label 读取
Top-1 / Top-3 / Top-5
coarse acc
target_adapt / target_test 无泄漏
```

没有这些，不要写模型。

## P1：复现 flat source-only

先跑 V0：

```text
h -> 64-class classifier
```

这是你后面所有方法的地基。

## P2：实现 hierarchical label

跑 V1：

```text
coarse group + fine offset
```

只要 V1 的 Top-3 / coarse acc / DBA 比 V0 稳，就说明“coarse beam semantics”这个故事有基础。

## P3：实现 shared-private

跑 V2 和 V3：

```text
V2: split 但无 decoupling loss
V3: split + orth + scene confusion/preservation
```

如果 V3 > V2，说明“不是名义上拆分”，解耦确实有效。

## P4：实现 target adapter

跑 label_budget = 10 / 50：

```text
Adapter-only vs Full fine-tuning
```

如果 adapter 接近 full FT，但参数远少，说明轻量适应有价值。

## P5：实现 prototype alignment

跑 label_budget = 0 / 5 / 10：

```text
Adapter-only vs Adapter+Proto
```

如果 0-label 和 5-label 有明显提升，这就是论文最有价值的结果之一。

---

# 16. 成功判据

这套想法值得继续做的最低标准：

```text
1. Hierarchical V1 相比 Flat V0：
   Top-3 或 coarse acc 明显提升；
   Top-1 不明显下降，最好也提升。

2. Decoupled V3 相比 V2：
   target scene source-only performance 更好；
   scene-from-c probe 更低；
   scene-from-s probe 更高。

3. Adapter+Proto 相比 Source-only：
   label_budget=0 时有提升；
   label_budget=5/10 时提升更明显。

4. Adapter+Proto 相比 Full FT：
   性能接近；
   trainable params < 2%；
   adaptation time 明显更短。
```

如果只满足 1 和 2，说明“知识解耦”方向可做，但 target adaptation 需要重设。

如果 1 不成立，说明 coarse label 设计可能有问题，需要尝试：

```text
K0 = 4
K0 = 16
angular smoothing
只用 GPS/camera/radar 的不同组合
```

如果 2 不成立，说明 shared/private 拆分没有起作用，需要加强：

```text
private_dim 从 64 降到 32
lambda_orth 从 0.02 提到 0.05
lambda_scene_c 从 0.05 提到 0.1
coarse head 严禁接触 private feature
```

如果 3 不成立，说明 prototype alignment 的伪标签噪声太大，需要：

```text
confidence_threshold 从 0.7 提到 0.8
只对 coarse group 做 entropy，不对 final beam 做 entropy
先 warmup 5 epochs adapter，再开 proto loss
```

---

# 17. 最小可发表验证版

最终你至少需要得到下面这一组实验：

```text
Dataset: DeepSense6G scenarios 31–34
Modalities: camera + radar + GPS
Splits: 4-fold leave-one-scene-out
Methods:
  V0 Flat
  V1 Hierarchical
  V2 Shared-private
  V3 Decoupled
  Full fine-tuning
  Adapter-only
  Adapter+Proto
Budgets:
  0 / 10 / 50 labels
Metrics:
  Top-1
  Top-3
  Top-5
  coarse acc
  trainable params
  adaptation time
```

先不要做：

```text
LiDAR
复杂 BEV
大 transformer
MAML
完整 AMBER 对比
复杂 MI loss
复杂 contrastive loss
```

这些都可以放到后续强化版。

---

# 18. 给 Codex 的一句总要求

请按“先验证假设、再扩展系统”的原则编码。所有模块必须通过 config 开关控制，所有实验必须保存 metrics、checkpoint、config 和 predictions。不要把模型写死成某一个 variant；要能通过同一套代码跑 flat、hierarchical、shared-private、adapter-only、adapter+proto 和 full fine-tuning。
