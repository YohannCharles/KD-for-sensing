_base_: _base.yaml
experiment:
  name: PCPF-T-stage1
model:
  primary:
    training_stage: stage1_expert
    fusion_mode: uniform
training:
  epochs: 40
  max_epochs: 40
  initialization_checkpoint: false
  validation:
    interval_epochs: 1
