已切到 `kd_mm_beam` 继续分析，并用它跑通了现有可视化入口。新产物在 [summary.json](/root/projects/KD-for-sensing/outputs/diagnostics/modality_visualization/summary.json:1)。

**核心判断**
scene32 的 image/radar/lidar 三模态失败，主要不是程序没加载数据，而是这三种模态在 scene32 上几乎没有可泛化信号，叠加 scene32 只有 15 个 `seq_index` 的序列级切分，train/test 域差异很重。

关键证据：

- scene32 中 image、radar、lidar 单模态最佳 top1 都是 `0.1202`，三模态 image+radar+lidar 也是 `0.1202`，且最佳 epoch 都在第 1 轮。这个数正好等于 test 集多数类 baseline。
- 同一 scene32 下 GPS/mmWave 正常：`gps=0.4504`，`mmwave=0.4597`，`radar_lidar_mmwave=0.4667`。所以不是 scene32 整体不可学，而是 image/radar/lidar 这组三模态不够。
- 三模态训练时 train acc 从 `0.1165` 升到 `0.3478`，但 val acc 没升，val loss 从 `3.38` 涨到 `5.46`，典型过拟合 train 序列。
- scene32 split 只有 train 10 条序列、test 5 条序列：[summary.json](/root/projects/KD-for-sensing/outputs/diagnostics/modality_visualization/summary.json:52) 和 [summary.json](/root/projects/KD-for-sensing/outputs/diagnostics/modality_visualization/summary.json:161)。我算到 future label 分布 train/test TVD 约 `0.1386`，明显大于当前 scene9 CSV 的 `0.0297`。
- 真实 Dataset 张量统计也支持这个结论：scene32 image motion mask 密度约 `0.009-0.013`，scene9 约 `0.021-0.025`；scene32 radar RA/DA std 约 `0.011/0.005`，scene9 test 约 `0.084/0.054`；scene32 LiDAR BEV 非零率约 `0.05`，scene9 约 `0.003`，且配置里 `lidar_normalize: false`：[modality_visualization.yaml](/root/projects/KD-for-sensing/configs/diagnostics/modality_visualization.yaml:18)。

**可视化程序评价**
现有程序有价值：它确实复用 Dataset 返回的处理后张量，并记录 split metadata、样本路径和统计信息：[modality_visualization.py](/root/projects/KD-for-sensing/src/kd_sensing/diagnostics/modality_visualization.py:80)。

但现在不够诊断这个问题：

- 默认每个 split 只抽 2 个样本：[modality_visualization.yaml](/root/projects/KD-for-sensing/configs/diagnostics/modality_visualization.yaml:38)，这次 test 两个样本都落在 `seq=14`，没有覆盖全部 test seq。
- 默认不显示 raw image：[modality_visualization.yaml](/root/projects/KD-for-sensing/configs/diagnostics/modality_visualization.yaml:44)，无法判断 motion mask 是否丢了关键场景信息。
- PNG 面板太小、留白大、标题有裁切，问题在当前固定网格布局：[modality_visualization.py](/root/projects/KD-for-sensing/src/kd_sensing/diagnostics/modality_visualization.py:433)。
- 没有跨 scene9/scene32、跨 train/test 的聚合统计，只靠单样本图很难看出 radar 对比度下降、LiDAR 密度突变、label 分布偏移。
- 没有模型预测叠加，无法直接看到 top-k 是否塌缩到多数类。

**修改建议**
优先改可视化为“诊断报告”，不是只画样本图：

1. 加 `split_stats.json`：按 scene/split/seq_index 汇总 image mask density、radar RA/DA std、LiDAR nonzero、label top-k、majority baseline、train/test 分布距离。
2. 加分层采样：支持 `per_seq_sample_count`，确保 scene32 test 的 `1,2,3,11,14` 都被采到。
3. raw + processed 同屏：raw image 序列、motion mask overlay、radar 共享色标、LiDAR 点数/ROI 前后点数/BEV 通道直方图。
4. 加 `compare_scenes: [9,32]` 模式，一次输出 scene9 vs scene32 的同表对比。
5. 可选加载 checkpoint，把 top-k 预测、是否命中、预测分布/混淆矩阵写进样本记录。
6. 修布局：用更大的 heatmap、`constrained_layout`、短标题、每行独立列数，避免现在 PNG 信息被压得太小。

当前最该先验证的实验是：对 scene32 做一个“随机窗口 split sanity check”或 k-fold/leave-one-seq-out。如果随机窗口 split 能明显升高，说明主因是序列级域迁移；如果仍然低，再重点查 image/radar/LiDAR 预处理。