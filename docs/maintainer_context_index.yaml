schema_version: 2
kind: maintainer_context_index
purpose:
  summary: >
    Minimal structured facts that are hard to infer directly from pyproject,
    OpenSpec, source paths, or project_surface_inventory. This is not a source
    tree mirror, entrypoint database, hotspot budget table, or runtime config.
retired_routes:
  config_tokens:
    - hist_beam
    - top8
    - residual
    - camera_residual
    - logits_kd
    - rkd
    - craf
    - marf
    - g2d
    - multimodal_nf
    - raymobtime
    - raymobtime_s008
    - bgam
    - gps_lidar_bgam
    - viewer_manifest
    - amr_net_gps_image
    - jepa_msac
    - run_amr_net_gps_image
    - run_jepa_msac
    - gps_circular_soft_label
    - mmw_sunny_modal15
  text_markers:
    - HiST-Beam
    - Top8 selector
    - GPS residual
    - camera residual
    - logits_kd
    - rkd
    - Raymobtime s008
    - raymobtime_s008
    - BGAM
    - viewer manifest
    - Gradio viewer
    - AMR-Net_gps_image
    - JEPA-MSAC
    - kd-sensing-run-amr-net-gps-image
    - kd-sensing-run-jepa-msac
    - scripts/mmw/visualize_gps_
    - gps_circular_soft_label
    - run_mmw_sunny_modal15
validation:
  focused:
    - openspec validate prune-overengineered-project-surface --strict
    - conda run -n kd_mm_beam pytest tests/test_architecture_boundaries.py -q
