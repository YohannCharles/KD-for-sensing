# 结果和 Claim 账本

本账本只保存源码可维护的摘要、配置路径、本地产物路径引用和 caveat。真实 checkpoint、metrics CSV、predictions、figures、feature cache、TensorBoard event 和训练日志仍留在 ignored 的 `outputs/`、`logs/`、`outputs/cache/` 或本地数据目录，不进入源码变更。

Claim status：

- `official reproduction`：官方数据、权重、源码、环境和官方 exact test packaging 均满足。
- `blocked official reproduction`：官方复现缺必要条件，不能填入伪造数值。
- `local substitute`：本仓库本地替代实验，可用于项目内对照，但不是官方结果。
- `local experimental baseline`：本仓库实验场景中的可训练 baseline，不声明论文官方复现。
- `local strict-validation`：checkpoint selection 不使用 test split；可作为本地 strict 证据。
- `upper-bound`：使用 test split、oracle 或其它上界信息选模，只用于上界诊断。
- `mock/smoke`：只验证代码路径或 schema。
- `not_comparable`：缺 strict comparability、paired seed、stress provenance 或统计证据，不能进入正式 claim。
- `historical ablation`：旧口径记录，不能覆盖 current summary。
- `unverified`：文档保留计划或入口，但当前未登记可引用数值。

| claim_id | model line | config / runner | run date / commit | dataset / split | target / metric field | value summary | checkpoint provenance | claim status | caveat |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `CLAIM-BB-OFFICIAL-BLOCKED` | Arnold22 official Table III | official BeamBench `challenge.py` plan via package CLI module | 2026-06-07, local audit commit `00b693b9...` | official raw test CSV unavailable locally | official DBA | no official metric reported | official AE/fusion weights missing; official exact packaging incomplete | `blocked official reproduction` | Do not fill paper or leaderboard numbers until official data, weights, source/config and environment are available. |
| `CLAIM-BB-TIII-CURRENT-BLOCKED` | Arnold22 Camera AE+GPS Direct current local substitute | `configs/fusion/beambench_image_ae_gps_direct.yaml`; `kd-sensing-run-beambench-image-ae-gps-tableiii` | current protocol defined by docs | DeepSense6G S32-S34 train, S31-S34 eval | `beam_target_source=current`, `paper_distance_angle`, linear `official_top3_dba` / Top-1/3/5 | no current-target strict numeric summary is promoted in this change | future run should write ignored checkpoint under `outputs/scenegroup_s32_s34/...` | `local substitute / unverified` | Current summary defines the protocol; old `future` target values below are historical ablations and cannot be copied as current Table III substitute. |
| `CLAIM-BB-FUTURE-STRICT-HIST` | Arnold22 local paper-split historical ablation | old `kd-sensing-run-beambench-image-ae-gps-tableiii --target-beam-source future` | 2026-06-08 log | DeepSense6G S32-S34 train, S31-S34 eval | future target, `official_top3_dba` | selected historical rows include weighted overall `0.5820`, `0.7594`, `0.7626` depending GPS/AE/checkpoint stage | local checkpoints under `outputs/beambench_image_ae_gps_direct_tableiii/...` | `historical ablation` | Kept for lineage only; not Table III strict setup after current-target correction. |
| `CLAIM-BB-UPPER-HIST` | Arnold22 local upper-bound | `--selection-split test_as_validation` historical runs | 2026-06-08 log | DeepSense6G S31-S34 test CSV used for selection | `official_top3_dba` | historical upper-bound examples include weighted overall `0.6282` and `0.7745` | local checkpoints under ignored `outputs/...` | `upper-bound` | Uses test CSV for checkpoint selection; not official unseen evaluation and not strict-validation evidence. |
| `CLAIM-TII-VLRG-LOCAL-PENDING` | TII-VLRG-style Transformer baseline | `configs/fusion/tii_vlrg_transformer_baseline.yaml` | pending | DeepSense6G configured scene | Top-K / DBA | no promoted numeric claim | local checkpoints and metrics only under ignored `outputs/analysis/local_baselines/tii_vlrg_transformer/` | `local experimental baseline / unverified` | 本地 VLRG-style Transformer baseline，不依赖 TII 官方源码、权重或 checkpoint；旧 external wrapper 只作可选导入参考。 |
| `CLAIM-JEPA-2604-LOCAL-001` | Image+GPS JEPA GPS-biased 2604-style | `configs/fusion/experiments/jepa_image_gps/image_gps_jepa_gps_biased_best_2604_s32_s34_lowmem.yaml` | local result recorded in `docs/experiment_matrix.md` before this split | DeepSense6G S32/S33/S34 stratified 80/10/10 | `2604_linear_topk`, DBA | S32/S33/S34 DBA `0.8777 / 0.8853 / 0.8796`, macro `0.8809` | local `best.pth` under ignored `outputs/scenegroup_s32_s34/...` | `local strict-validation` | State as local 2604-style split, not proof over the original paper exact split because exact split index/seed is unavailable. |
| `CLAIM-JEPA-BBFAIR-PENDING` | Image+GPS JEPA BeamBench-fair family | `configs/fusion/experiments/jepa_image_gps/*beambench_fair_lowmem.yaml` | pending | DeepSense6G S32-S34 train, S31-S34 test | current target, `paper_distance_angle`, BeamBench linear DBA | no promoted numeric claim in this registry | local checkpoints only | `unverified` | Must compare GPS-biased, GPS-query, supervised and random controls within the same BeamBench-fair family. |
| `CLAIM-JEPA-QUERY-PENDING` | GPS-query pooling derivative | `image_gps_jepa_gps_query_pool_best_*` | pending | BeamBench-fair or 2604-style family | family metric profile | no promoted numeric claim | local checkpoints only | `unverified` | Do not mix BeamBench-fair and 2604-style claims. |
| `CLAIM-JEPA-PREDICTIVE-ROBUSTNESS-PENDING` | JEPA predictive hybrid robustness | `configs/fusion/experiments/jepa_image_gps/image_gps_jepa_predictive_hybrid_beambench_fair_lowmem.yaml`; `configs/diagnostics/jepa_gps_shortcut_benchmark_predictive_robustness_smoke.yaml` or local real manifest; `kd-sensing-jepa-gps-shortcut-benchmark` | pending | DeepSense6G S32-S34 train, S31-S34 test; train profile legacy `P4_joint_predictive_recovery`; clean anchor + `image_missing` / `image_noise` / `gps_noise` stress curves | `predictive_dba`, `predictive_top1`, `S@drop<=0.02`, `S@drop<=0.05`, `AUC_retention`, `collapse_s`, `weakest_axis`, `margin_vs_resnet_dba`, `overall_cxd_dba` sanity | no promoted numeric claim | local checkpoints only; smoke uses synthetic metrics and mock paths | `unverified / mock/smoke pending` | Single legacy P4 training profile 不等价于完整 stress-curve benchmark. Only a real strict comparable train-then-evaluate run with clean anchor, each default stress curve condition-level metrics, strict comparability fields, difficulty digest, seed, split, sample_count and audited Image ResNet+GPS baseline may become a real claim, and only when `margin_vs_resnet_dba >= 0.05`; otherwise status stays `pending`, `unavailable`, `not_comparable`, `clean_anchor_unstable` or `mock/smoke`. |
| `CLAIM-AMBER-LITE-PENDING` | AMBER-lite missing-modality baseline | `configs/fusion/amber_lite_missing_modality.yaml`; `configs/diagnostics/amber_lite_missing_modality_eval.yaml` | pending | DeepSense6G configured scene; image/radar/GPS/LiDAR | condition-level Top-K、DBA、beam distance | no promoted numeric claim | local checkpoints and metrics only under ignored `outputs/analysis/local_baselines/amber_lite_missing_modality/` | `local experimental baseline / unverified` | 默认 scratch ResNet，不下载外部权重；这是项目 baseline，不是完整 AMBER 官方复现。缺 real metrics 或 strict comparability fields 时保持 `pending`、`unavailable` 或 `not_comparable`。 |
| `CLAIM-AMBER-FULL-PENDING` | AMBER full architecture reproduction | `configs/fusion/amber_full_architecture.yaml` | pending | DeepSense6G configured scene; image/radar/GPS/LiDAR | condition-level Top-K、DBA、beam distance；AMBER L2/CMA diagnostics | no promoted numeric claim | local checkpoints and metrics only under ignored `outputs/analysis/local_baselines/amber_full_architecture/` | `paper-aligned local architecture reproduction / unverified` | 本地 full architecture reproduction，当前为四模态、无历史 beam token、ResNet18 spatial-token pretrained 配置和 class-query CMA payload；缺 real metrics、official weights/source 或 strict comparability fields 时不得写成 official AMBER reproduction。 |
| `CLAIM-RBMA-MISSING-PENDING` | Weighted-sum / RBMA missing-modality workflow | `configs/fusion/experiments/rbma_missing_workflow/*fullrun.yaml`, `weighted_sum_*.yaml`; `kd-sensing-eval-u-mask-matrix`; `scripts/summarize_missing_runs.py` | pending | DeepSense6G configured scene; internal `image/radar/lidar/gps`; pattern-balanced masks; single-job default runner | hard-label Top-1/3/5/loss/ADBA/MAE by missing pattern; timing data/step/memory; prototype and full-to-partial KD diagnostics | no promoted numeric claim | local checkpoints/metrics only under ignored `outputs/scene31/`, `outputs/scene31/logs/` and `outputs/scene31/eval/` | `local experimental baseline / unverified` | U-MaskBeamJEPA current/local opt-in workflow；weighted_sum/AMBER-style mask 是下一轮主线，RBMA 只作对照；strong-encoder 和 M2Beam single-modal overlay 只保留为 local/manual checkpoint-placeholder 输入；prototype target 是 supervised beam topology，full-to-partial teacher 是 current stabilization，不恢复 legacy `logits_kd`/`rkd`/distillation runtime，也不声明 AMBER official 数值。 |
| `CLAIM-MISSING-MODALITY-STRESS-PENDING` | Missing-modality statistical/stress evidence gate | `kd_sensing.diagnostics.missing_modality_statistics`; `kd_sensing.diagnostics.missing_modality_stress`; eval matrix CSV/JSON inputs | pending | Scene31/RBMA/AMBER/RMBP-MM local summaries or explicit stress manifest | primary metric with mean/std/CI, paired delta, win/loss/tie, strict comparability, stress suite status | no promoted numeric claim | outputs remain under ignored `outputs/analysis/missing_modality_stress/` or explicit local dirs | `pending / not_comparable until evidence complete` | Gate 只输出 `statistical_claim_ready`、warnings 和 next actions，不自动修改本 registry。单 seed、mock/smoke、unpaired seed、CI 跨阈值、缺 stress provenance 或 strict fields 时保持 `pending`、`not_comparable`、`mock/smoke` 或 `unverified`。 |
| `CLAIM-BEV2604-PENDING` | BEV-Fusion 2604 reproduction | `configs/fusion/experiments/bev_fusion_2604/paper_full.yaml` | pending | DeepSense6G S32/S33/S34 stratified 80/10/10 | `2604_linear_topk` | no promoted numeric claim | local checkpoint path only when run | `unverified` | `low_memory` is paper approximation and `smoke` is mock/schema only. |
| `CLAIM-RMBP-MM-PENDING` | RMBP-MM missing-modality baseline | `configs/fusion/experiments/wcl2025_missing_modality/local_substitute.yaml` | pending | DeepSense6G configured scene; image/radar/GPS/LiDAR/mmWave | condition-level Top-K / DBA / beam distance | no promoted numeric claim | local checkpoints and metrics only under ignored `outputs/analysis/local_baselines/rmbp_mm/` | `local experimental baseline / unverified` | 本地五模态缺失模态 baseline，不依赖 WCL 官方 artifact；`kd-sensing-wcl2025-missing-modality-audit` 只作可选 source-audit 背景。 |
| `CLAIM-MMW-GPSV2-PENDING` | MMW Town GPS v2 | `configs/mmw_town_gps_adapter_v2.yaml` | pending | Town10 sunny group-safe split | circular error, DBA, TopK | no promoted numeric claim | local outputs under `outputs/analysis/mmw_town_gps_adapter_v2/` | `unverified` | `within_scene_train` is sanity/upper-bound style evidence, not cross-scene generalization. |
| `CLAIM-PHYSICS-MMW-LOCAL-001` | Physics-informed MMW beam baseline | `configs/fusion/physics_informed_mmw_*.yaml`; `kd-sensing-inspect-mmw-physics`; `pinn_multimodal_beam` | 2026-06-29 user-provided local summary | MMW Town10 prepared data; config-declared split | val_acc / val_adba | image-only `0.7255 / 0.9555`; sparse CSI only `0.5126 / 0.7637`; oracle full CSI only `0.6765 / 0.9689`; image+sparse CSI no-physics `0.7983 / 0.9793`; task-aligned PINN no-CSI-recon `0.8165 / 0.9760`; no beam power `0.7941 / 0.9764`; no array `0.7689 / 0.9739`; full raw CSI reconstruction collapsed to `0.2843 / 0.5990` | local outputs/checkpoints only under ignored `outputs/`; exact run root not recorded in source | `local experimental baseline` | Sparse CSI provides the main multimodal gain; task-aligned physics gives modest exact Top-1 improvement while ADBA is slightly lower than no-physics; array consistency is the strongest useful physics term; raw CSI reconstruction is negative transfer and should stay disabled for main conclusions. `oracle_full` remains upper-bound only. |
| `CLAIM-CSI-HARDENING-PENDING` | CSI hardening / GPS+CSI matrix | `configs/csi/hardening_matrix/` and `configs/fusion/csi_hardening_matrix/` | pending | configured CSI / GPS+CSI splits | TopK, DBA, loss | no promoted numeric claim | local run roots only | `unverified` | Debug matrix validates inheritance/parity; full matrix claims need explicit run provenance. |
| `CLAIM-JEPA-SHORTCUT-SMOKE` | JEPA shortcut benchmark smoke | `configs/diagnostics/jepa_gps_shortcut_benchmark_smoke.yaml` | current smoke | synthetic benchmark metrics | synthetic robustness schema | no research metric | no checkpoint | `mock/smoke` | Smoke only verifies runner, aggregation and visual-analysis ingestion. |
| `CLAIM-JEPA-SHORTCUT-PENDING` | JEPA shortcut benchmark BeamBench-fair | `configs/diagnostics/jepa_gps_shortcut_benchmark_beambench_fair.yaml` | pending | local checkpoint matrix | robustness/drop/shortcut reliance | no promoted numeric claim | checkpoint paths are local placeholders | `unverified` | Replace weights with audited local runs; do not commit metrics/figures/cache. |
| `CLAIM-SCENARIO-D-SMOKE` | Scenario D image observability benchmark | `configs/diagnostics/jepa_gps_shortcut_benchmark_scenario_d_smoke.yaml` | current smoke | synthetic metrics only | C0-C4 x D0-D7 phase/crossing/decomposition artifact schema | no promoted numeric claim | no checkpoint | `mock/smoke` | Validates Scenario D manifest, shared difficulty provenance, `results/cxd_phase_diagram.csv`, `results/cxd_phase_heatmap.npy`, `results/modality_dominance.csv`, `results/crossing_region_Cx_Dy.json`, `results/failure_mode_decomposition.csv` and PNG registration only; dominance rows are mock/unavailable unless real diagnostics are supplied, and real benchmark outputs stay under ignored `outputs/analysis/scenario_d_image_observability/`. |
| `CLAIM-JEPA-VIS-DIAG` | JEPA visual analysis | `configs/diagnostics/jepa_visual_analysis_2604.yaml` | diagnostic-only | existing local artifacts | analysis tables/figures | no standalone performance claim | reads local checkpoints/cache | `diagnostic-only` | Attention, UMAP and nearest-neighbor evidence must be paired with task metrics; not standalone causal proof. |

## Claim 更新规则

新增或更新 claim 时必须记录：

- config path 或 runner command；
- run date 或 commit；
- dataset/scenes、split protocol、selection split；
- target source、metric field、label space；
- checkpoint provenance 和本地产物路径；
- claim status 与 caveat。
- 缺失模态 claim 还必须记录 seed_count、baseline、primary metric、mean/std 或 CI、paired comparison 状态、strict comparability status、stress suite status、difficulty_digest 和 warning/next action。

如果结果来自 mock、smoke、upper-bound、historical ablation 或 blocked official reproduction，必须在同一行显式标记，不能在 README、实验矩阵或论文草稿中写成正式结果。

统计/stress gate 只能辅助人工升级 claim：`statistical_claim_ready=true` 不会自动改写本账本；缺任一必要证据时，claim status 继续保持 `pending`、`unverified`、`not_comparable` 或 `mock/smoke`。

AMR-Net_gps_image、JEPA-MSAC、BGAM、BGAM-only Top8 candidate manifest、viewer manifest、固定 shell orchestration 和 Gradio viewer 已退役；旧 pending/mock/blocked claim 行不再作为 current claim 占位保留。历史本地产物如仍在 `outputs/` 中，只能作为 archive 背景或人工清理候选。
