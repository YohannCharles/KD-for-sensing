# OpenSpec 任务上下文

current specs 只描述 MMW T2/baseline 闭包与其运行、配置、入口、健康和历史边界。退役能力保留在 archive 与 `docs/retired_routes.md`，不能以 current spec 的形式继续要求实现。

变更前先读 active change 的 proposal、design、tasks 和相关 current spec；实现范围改变时先更新 artifact。验证使用 `openspec validate <change> --strict`，收口前使用 `openspec validate --all --strict`。
