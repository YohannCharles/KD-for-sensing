结论先说：**这版 CRAF 还没有真正学到“模态可靠性”，目前更像是一个高容量 all-modal 模型，在 warmup 后被反事实 gate 和 auxiliary loss 带偏了。**

从曲线看，CRAF 有潜力，但当前训练策略需要调整。

---

# 1. 当前结果说明了什么？

从截图读数大概是：

| 模型                             |  train acc |    val acc | val_atop3 |   val_adba |
| ------------------------------ | ---------: | ---------: | --------: | ---------: |
| CRAF all modalities            | **0.4879** | **0.3921** |   约 0.706 |      0.878 |
| GPS teacher                    |     0.4114 | **0.4411** |   约 0.769 | **0.8985** |
| mmWave teacher                 |     0.4777 | **0.4492** |   约 0.75+ |      0.888 |
| radar+gps+lidar+mmwave teacher |     0.4174 |     0.4236 |   约 0.72+ |     0.8658 |
| image teacher                  |     0.2806 |     0.1004 |         低 |      0.375 |
| lidar teacher                  |     0.2405 |     0.0315 |         低 |     0.2116 |
| radar teacher                  |     0.2159 |     0.0724 |         低 |      0.370 |

最明显的问题是：

**CRAF 的 train acc 最高，但 val acc 明显低于 GPS 和 mmWave。**

这不是模型能力不够，而是：

> all-modal CRAF 在训练集上学得很强，但验证集上发生了负迁移 / 过拟合 / gate 失效。

更关键的是，CRAF 的 val 曲线早期其实冲到过大约 **0.45–0.47**，后面掉到了 **0.39** 左右。你说 warmup 是前 5 轮，这和曲线很吻合：**warmup 之后，开启 counterfactual gate，模型性能开始不稳定并逐步下降。**

---

# 2. 最大异常：reliability 没有学对

按你的单模态结果，期望 reliability 大概应该是：

```text
mmWave / GPS 高
image / lidar / radar 低
```

但当前 CRAF 末尾 reliability 大概是：

| 模态     | reliability smoothed |
| ------ | -------------------: |
| mmWave |           **0.5821** |
| lidar  |           **0.5408** |
| image  |               0.4867 |
| GPS    |               0.4728 |
| radar  |               0.4604 |

这个排序很不合理。

尤其是：

* `lidar_teacher` val acc 只有 **0.0315**，但 reliability 是 **0.54**；
* `image_teacher` val acc 只有 **0.1004**，但 reliability 和 GPS 接近；
* GPS 单模态很强，但 reliability 反而低于 LiDAR；
* 所有 reliability 都集中在 **0.46–0.58**，区分度太小。

这说明当前 gate 没有真正做到“抑制弱模态”。它只是给所有模态一个差不多的 0.5 权重。

所以 CRAF 实际上仍然在吃大量弱模态噪声。

---

# 3. warmup 5 轮太短

从 loss 曲线看，counterfactual gate loss 在第 5 轮附近开始出现，然后 reliability 曲线发生明显变化。

这说明：

> 可靠性监督是在主模型还没稳定时就开始生成了。

这会导致一个问题：

早期模型自己都还没有学好，`Loss(S)` 和 `Loss(S \ m)` 的差值非常噪声。
这时候得到的 `Δ_m` 不是“真实模态贡献”，而是“未收敛模型的随机波动”。

所以 gate 可能会学到错误结论，例如：

```text
删除 LiDAR 后 loss 暂时升高 → 误以为 LiDAR 很有用
删除 GPS 后 loss 没怎么变 → 误以为 GPS 不重要
```

这和你现在的 reliability 排序非常像。

---

# 4. 另一个问题：unimodal auxiliary loss 太大

你这里：

```text
loss/train_unimodal_aux ≈ 2.30
```

如果权重是 0.1，那么它对总 loss 的贡献约为：

```text
0.23
```

这并不小。

但是 image / lidar / radar 单模态本身在场景 32 上很弱。你持续给它们 unimodal auxiliary loss，等于一直强迫模型去优化这些弱分支。

这会带来两个问题：

1. 弱模态 encoder 被迫拟合训练集，容易过拟合；
2. reliability estimator 可能被弱模态 head 的训练信号污染。

所以 CRAF 训练集上升很快，但验证集掉下来。

---

# 5. beam soft loss 也可能偏大

`loss/train_beam_soft ≈ 2.98`。

如果权重是 0.1 或 0.2，它也会贡献大约：

```text
0.30 ~ 0.60
```

对于 Top-1 accuracy 来说，这可能太强。

beam-aware soft loss 有价值，但现在你的主要目标是先让 CRAF 的 Top-1 不低于 GPS/mmWave teacher。建议先把它降下来，不要让它主导优化。

---

# 6. 当前 CRAF 的主要问题总结

我会把问题定位为四个：

## 问题 1：warmup 太短

5 轮后模型还没稳定，反事实贡献 `Δ_m` 噪声太大。

## 问题 2：gate target 可能过于模糊

如果 `Loss(S)` 和 `Loss(S\m)` 差值很小，那么：

[
target = \sigma(\Delta_m / \tau)
]

会接近 0.5。

结果就是所有 reliability 都在 0.5 附近，无法真正区分模态。

## 问题 3：独立 sigmoid gate 区分度不足

现在每个模态独立输出一个 0–1 分数，但最后都在 0.5 左右。
这种 gate 不会形成强选择。

## 问题 4：弱模态 auxiliary loss 持续干扰

image / lidar / radar 单模态已经证明很弱，但它们的 auxiliary loss 仍然在持续参与训练。

---

# 7. 优先级最高的改进

我建议你下一版不要一次改太多。先做下面这些。

---

## 改进 1：把 warmup 从 5 轮改到 20–30 轮

建议：

```yaml
training:
  warmup_epochs: 25
  cf_start_epoch: 26
```

或者更稳：

```text
先训练 token-transformer all-modal baseline，r_m = 1。
当 val acc 连续 5 轮不提升后，再开启 counterfactual gate。
```

也就是说，反事实可靠性不要从第 5 轮就开始。

当前曲线里，CRAF 前期其实很好，说明 backbone 没问题。真正的问题是 gate 太早介入。

---

## 改进 2：warmup 阶段 gate 固定为 1

前 20–30 轮：

[
r_m = 1
]

不要使用 reliability estimator 的输出。

伪代码：

```python
if epoch < warmup_epochs:
    r = torch.ones_like(r)
else:
    r = reliability_estimator(...)
```

这样先让 feature extractor 和 fusion backbone 学到稳定预测，再让 gate 判断模态贡献。

---

## 改进 3：counterfactual target 加 ignore band

现在不要让所有 `Δ_m` 都参与监督。

如果：

[
\Delta_m = Loss(S \setminus m) - Loss(S)
]

很小，说明删除这个模态影响不明确。不要强行把它映射到 0.5。

建议：

```python
eps = 0.03  # 或 0.05

if delta > eps:
    target = 1.0
elif delta < -eps:
    target = 0.0
else:
    ignore this sample-modality pair
```

也就是只监督明确有帮助或明确有害的样本。

这样比连续 sigmoid 更稳定。

当前 reliability 全部挤在 0.5 附近，很可能就是因为大量 `delta` 接近 0，导致 gate 学不到区分度。

---

## 改进 4：反事实 loss 只用 CE，不要用 beam soft / aux

计算模态贡献时，只用主任务 CE：

```python
delta_m = CE_drop_m - CE_full
```

不要把下面这些加进去：

```text
beam soft loss
unimodal auxiliary loss
KD loss
gate loss
```

因为我们要判断的是：

> 这个模态是否提升 beam classification 主任务。

如果把 beam soft 或 aux 混进去，`Δ_m` 会变得不干净。

---

## 改进 5：把 unimodal auxiliary loss 改成 warmup-only

建议：

```yaml
loss:
  uni_weight_warmup: 0.03
  uni_weight_after_warmup: 0.0
```

或者最多：

```yaml
loss:
  uni_weight_after_warmup: 0.01
```

不要后期还强迫 image / lidar / radar 单模态 head 学好。

场景 32 里它们单独训练已经很差，后期继续给 auxiliary loss，反而会污染融合模型。

---

## 改进 6：beam soft loss 先降到 0.05

建议先改成：

```yaml
loss:
  beam_soft_weight: 0.05
```

等 CRAF 的 Top-1 稳定超过 mmWave/GPS 后，再调大 beam soft loss 去优化 DBA。

当前应该先救 Top-1。

---

# 8. gate 结构也建议改

现在 independent sigmoid 的问题是：即使 mmWave 是 0.58，radar 是 0.46，差距也不大。弱模态仍然大量进入 Transformer。

我建议改成 **softmax-normalized modality gate**。

## 当前形式

[
g_m = \sigma(s_m)
]

## 建议形式

对当前可用模态做 softmax：

[
\alpha_m = \frac{\exp(s_m / T_g)}{\sum_{j \in S} \exp(s_j / T_g)}
]

然后为了保持 token 幅值：

[
g_m = |S| \cdot \alpha_m
]

最终：

[
\tilde z_{m,\tau} = g_m z_{m,\tau}
]

这样有两个好处：

1. 模态之间会形成竞争；
2. 弱模态不会都保持在 0.5 附近。

建议温度：

```yaml
gate_temperature_start: 1.0
gate_temperature_end: 0.3
```

前期平滑，后期更有选择性。

---

# 9. 更推荐的反事实贡献定义

你现在大概率用的是：

[
\Delta_m = L(S \setminus m) - L(S)
]

也就是从 all-modal 里删除某个模态。

这个可以用，但有一个问题：

> 如果 full set 本身已经被弱模态污染，那么从 full set 里删除一个模态得到的贡献不一定可靠。

更好的方式是随机上下文贡献：

[
\Delta_m(A) = L(A) - L(A \cup {m})
]

其中 (A) 是一个不包含模态 (m) 的随机模态子集。

解释：

* 如果加入 (m) 后 loss 下降，说明 (m) 有帮助；
* 如果加入 (m) 后 loss 上升，说明 (m) 有害。

这比只做 leave-one-out 更通用，因为它能判断某个模态在不同组合下的边际贡献。

伪代码：

```python
A = sample_subset_without_m(available_modalities)
A_plus_m = A | {m}

loss_A = CE(model(A), y)
loss_A_plus_m = CE(model(A_plus_m), y)

delta = loss_A - loss_A_plus_m
```

然后：

```python
if delta > eps:
    target = 1
elif delta < -eps:
    target = 0
else:
    ignore
```

这个定义更适合你的论文故事：

> 模态不是静态强弱，而是在不同组合下有条件贡献。

---

# 10. 建议的下一版配置

可以先按这个跑一版：

```yaml
training:
  epochs: 100
  warmup_epochs: 25
  cf_start_epoch: 26

  modality_dropout:
    enabled: true
    drop_prob: 0.15
    min_keep: 1

  counterfactual:
    enabled: true
    mode: "context_marginal"   # 建议新增
    num_drop_per_batch: 1
    use_ce_only: true
    ignore_delta_eps: 0.03
    no_grad_drop_forward: true

loss:
  ce_weight: 1.0
  beam_soft_weight: 0.05
  gate_weight: 0.2
  gate_ramp_epochs: 10

  uni_weight_warmup: 0.03
  uni_weight_after_warmup: 0.0

model:
  reliability:
    enabled: true
    gate_type: "softmax"
    gate_temperature_start: 1.0
    gate_temperature_end: 0.3
    min_gate: 0.05
```

gate weight 不要一开始就 0.2，建议 ramp：

```python
gate_weight_epoch = gate_weight * min(1.0, (epoch - cf_start_epoch + 1) / 10)
```

---

# 11. 必须加的 debug 日志

你现在只有 reliability，但还不够。必须加下面这些。

## 每个模态的反事实贡献

```text
cf/delta_mean_image
cf/delta_mean_lidar
cf/delta_mean_radar
cf/delta_mean_gps
cf/delta_mean_mmwave
```

预期：

```text
delta_mmwave > 0
delta_gps > 0
delta_image 接近 0 或 < 0
delta_lidar 接近 0 或 < 0
delta_radar 接近 0 或 < 0
```

如果不是这个趋势，说明反事实贡献计算有问题，或者模型还没稳定。

---

## 每个模态的 target 均值

```text
cf/target_mean_image
cf/target_mean_lidar
cf/target_mean_radar
cf/target_mean_gps
cf/target_mean_mmwave
```

如果所有 target 都在 0.45–0.55，gate 肯定学不出东西。

---

## 每个模态被监督的比例

```text
cf/target_valid_rate_image
cf/target_valid_rate_lidar
cf/target_valid_rate_radar
cf/target_valid_rate_gps
cf/target_valid_rate_mmwave
```

加入 ignore band 后，要确认不是大部分样本都被 ignore。

---

# 12. 必须做的消融实验

下一步不要直接只跑 full CRAF。建议按顺序跑：

## A. Token Transformer，无 gate

```yaml
reliability.enabled: false
uni_weight: 0.0 或 0.03 warmup only
beam_soft_weight: 0.05
```

目的：确认 token-transformer backbone 的上限。

如果这个模型能到 0.44–0.46，说明 backbone 没问题。

---

## B. CRAF，但不启用 counterfactual

```yaml
reliability.enabled: true
cf_start_epoch: 999
```

或者直接：

```text
r_m = 1
```

目的：确认性能下降是不是 counterfactual gate 导致的。

---

## C. CRAF，warmup 25，开启 gate

这是你真正要比较的版本。

---

## D. CRAF，固定强模态 prior

先做一个 sanity check：

```text
mmwave: 1.0
gps: 0.9
image: 0.2
lidar: 0.2
radar: 0.2
```

如果这个版本显著提升，说明“抑制弱模态”这条路是对的，只是当前 learned reliability 没学好。

如果固定 prior 也不提升，说明融合 backbone 或数据处理还有问题。

---

# 13. 结构层面的进一步改进

如果上面训练改完还不稳定，我建议把融合结构从：

```text
所有模态 × 所有时间 token 一起进 Transformer
```

改成层级结构：

```text
每个模态内部先做 temporal encoder
        ↓
每个模态得到一个 modality token
        ↓
reliability gate
        ↓
cross-modal Transformer
        ↓
horizon query prediction
```

也就是：

[
x_{m,1:W} \rightarrow z_m
]

然后只对：

[
{z_{image}, z_{lidar}, z_{radar}, z_{gps}, z_{mmwave}}
]

做跨模态融合。

这样有两个好处：

1. 弱模态不会因为 token 数量多而污染 attention；
2. reliability gate 更直接，因为一个模态就是一个 token。

当前 5 个模态、每个 W 帧，如果 W=8，就是 40 个 token。
其中 image/lidar/radar 弱模态占 24 个 token，GPS/mmWave 强模态只占 16 个 token。
如果没有强 gate，attention 很容易被弱模态噪声干扰。

---

# 14. 对这版结果的最终判断

这版 CRAF 的核心信息是：

**方法方向有潜力，但当前 gate 训练失败了。**

证据是：

1. CRAF train acc 高，说明容量足够；
2. CRAF val 早期冲高，说明 backbone 能学到有效融合；
3. warmup 后 val 下滑，说明 counterfactual / gate 训练引入了不稳定；
4. reliability 排序不符合单模态表现，说明 gate 没学到真实模态可靠性；
5. weak modalities 没有被压低，导致 all-modal 负迁移仍然存在。

下一版最重要的是：

```text
延长 warmup
降低 aux / beam soft 干扰
让 counterfactual target 更干净
给 delta 加 ignore band
把 sigmoid gate 改成 softmax competitive gate
记录 delta 和 target，检查可靠性监督是否合理
```

我建议你下一次先跑这个最小改进版：

```text
warmup 25
CE-only counterfactual
ignore band eps=0.03
uni aux warmup only
beam soft weight=0.05
softmax modality gate
gate weight 0.2 with 10-epoch ramp
```

如果这版 reliability 最终变成：

```text
mmWave / GPS 明显高
image / lidar / radar 明显低
```

并且 val acc 不再从 0.46 掉到 0.39，那么 CRAF 的主线就基本跑通了。
