我按 `openspec-explore` 只做了调查，没有改代码。对比对象是上游仓库 [WillysMa/KD-for-sensing](https://github.com/WillysMa/KD-for-sensing)，本地没有 git remote；我克隆了一份到 `/tmp/KD-for-sensing-original` 做只读对比。当前 OpenSpec 没有活动 change，`pytest` 全量通过：`169 passed in 11.04s`。

**主要问题**
1. **当前默认 GRU 层数和上游/随附权重不兼容。**  
   本地配置统一成了 `gru_params: [64, 64, 2]`，例如 [configs/image/teacher_no_kd.yaml](/root/projects/KD-for-sensing/configs/image/teacher_no_kd.yaml:31) 和 [configs/fusion/image_radar_student_no_kd.yaml](/root/projects/KD-for-sensing/configs/fusion/image_radar_student_no_kd.yaml:38)。  
   上游 `train_image.py` 默认 teacher/student 都是 1 层 GRU，`train_both.py` 默认 teacher 2 层、student 1 层。随附 `All_models/*Std*.pth` 也都是 1 层 student。用当前配置加载这些权重时会缺少：
   `GRU.weight_ih_l1`, `GRU.weight_hh_l1`, `GRU.bias_ih_l1`, `GRU.bias_hh_l1`。  
   这表示第二层 GRU 是随机初始化的，不是原权重。

2. **权重加载使用 `strict=False`，会静默掩盖上面的问题。**  
   Teacher 加载在 [trainer.py](/root/projects/KD-for-sensing/src/kd_sensing/engine/trainer.py:65)，评估加载在 [evaluator.py](/root/projects/KD-for-sensing/src/kd_sensing/engine/evaluator.py:54)。现在不会报告 missing/unexpected keys。  
   如果拿旧 `All_models/ImageTeacher_best.pth` 或 `ImageStd*.pth` 评估/蒸馏，结果可能看起来能跑，但模型并非完整加载。

3. **`resume` 配置目前基本是死参数。**  
   默认配置暴露了 `resume` 和 `start_epoch`：[defaults.py](/root/projects/KD-for-sensing/src/kd_sensing/config/defaults.py:97)，训练循环只读取 `start_epoch`，没有调用 `load_checkpoint`：[trainer.py](/root/projects/KD-for-sensing/src/kd_sensing/engine/trainer.py:177)。  
   上游脚本有 resume checkpoint 加载逻辑；本地重构后这块丢了。

4. **复现实验的超参数和随附 params 文件漂移较大。**  
   当前很多配置是 batch size `3`、seed `0`、lr `0.00075`。随附参数文件里常见是 batch size `32`、seed `42`，并且不同实验 lr/temperature/alpha 不同，例如 ImageStd_noKD lr `0.001`，BothStd_noKD lr `0.0004`，BothStd_KD lr `0.00095`。  
   如果目标是复现 `All_models/Performance.txt`，当前 canonical 配置不能直接视为原实验配置。

5. **部分“可配置参数”实际被架构硬编码限制。**  
   `image_size` 虽在配置里暴露，但 motion mask 仍硬编码 `224x224`：[transforms.py](/root/projects/KD-for-sensing/src/kd_sensing/data/transforms.py:46)。Image/Fusion teacher 的 FC 也假定 224 输入。Radar extractor 同样假定 RA/DA 尺寸对应 `128x64`。这些参数改动后很可能 shape break。

**架构对齐情况**
数据流基本保留了上游语义：image 用 7 个 motion mask 加 3 个 zero future，radar 用 8 帧 RA/DA 加 2 个 zero future，最后取 `num_pred + 1` 个输出做 loss；本地实现见 [batch.py](/root/projects/KD-for-sensing/src/kd_sensing/engine/batch.py:36)。模型类本身和上游对应实现大体一致，真正的偏离主要来自配置矩阵、checkpoint 兼容策略和训练工作流。

我的建议是先明确目标：如果要“复现原代码/原权重”，需要恢复 legacy 配置的一层 GRU、batch/lr/seed/KD 参数，并把权重加载改成显式报告 missing keys；如果要继续当前二层 GRU canonical 路线，就应把旧 `All_models` 明确隔离为 legacy，不要让 `strict=False` 静默混用。